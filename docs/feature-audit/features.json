[
  {
    "id": "WEB-01",
    "area": "web",
    "feature": "Marketing nav (scroll-reveal sticky bar)",
    "user_story": "As a visitor, I can use a top navigation bar that appears after I start scrolling so that I can jump to sections or sign in without it covering the hero wallpaper.",
    "expected_behavior": "Fixed top bar starts hidden (opacity 0, translated up, pointer-events-none) and slides in once window.scrollY exceeds 60% of viewport height, hiding again on scroll back up. Holds the dhizume Wordmark, anchor links #how/#pricing/#faq (each with a pose icon, hidden below md), a 'sign in' link, and a 'get started' Button — both linking to /signin.",
    "source_refs": "web/components/marketing/Nav.tsx:24-78",
    "verify_method": "Playwright against local dev server: load /, assert header is hidden at top, scroll past 60vh and assert it becomes visible, click each anchor and assert hash navigation; assert 'sign in' and 'get started' both point to /signin.",
    "notes": "adversarial=stale_test: PRODUCT fine — Hero 'get started'->/signin visible+clickable in first fold (landing.spec.ts:45 passes); scroll-reveal nav is intentional redesign (commits 1eb7101/7a6caf5, Nav.tsx:13-15). FIX=update the test (scroll past 60vh before header click, or assert hero CTA), not the product | FIXED: stale test updated; landing.spec 4/4 green.",
    "p2_test_status": "fail",
    "p2_evidence": "Static read Nav.tsx:24-78 confirms scroll-reveal (hidden opacity-0 -translate-y-full pointer-events-none at top, shown once scrollY > innerHeight*0.6), Wordmark + #how/#pricing/#faq pose-icon anchors (hidden below md), a 'sign in' Link and a 'get started' Button both -> /signin. BUT Playwright landing.spec.ts:32 'has a sign-in affordance linking to /signin' FAILED: header sign-in link never enters viewport at the top fold (pointer-events-none + translated out), so .click() times out — a returning user has no clickable sign-in until they scroll past 60vh.",
    "error_found": "landing.spec.ts:32 RED — header 'sign in' link is off-screen (Nav scroll-reveal, -translate-y-full + pointer-events-none until scrollY>60vh) so .click() times out.",
    "severity": "low",
    "error_type": "ux",
    "p3_fix_status": "done",
    "p3_fix_ref": "web/e2e/landing.spec.ts:32 — reveal scroll-reveal nav (scrollTo 0.7*vh) before clicking header sign-in",
    "p4_retest_status": "pass"
  },
  {
    "id": "WEB-02",
    "area": "web",
    "feature": "Hero (wallpaper fold)",
    "user_story": "As a visitor, I can see a clean hero over the throne video with one tagline and a CTA so that the first screen reads as a living wallpaper, not a busy page.",
    "expected_behavior": "Full-viewport (min-h-dvh) section, content bottom-aligned: a single tagline line, a 'get started' Button linking to /signin, and an animated bounce scroll-cue chevron (aria-hidden). Elements fade up via the 'rise' animation with staggered delays.",
    "source_refs": "web/components/marketing/Hero.tsx:8-27",
    "verify_method": "Playwright on local dev: load /, assert the tagline text and a get-started link to /signin are present in the hero; static read confirms the scroll cue is aria-hidden.",
    "notes": "Checklist's 'BubbleMock preview' and 'live badge' are NOT in Hero — the chat preview and 'live now' badge live in PhoneShowcase (WEB-03) | Catalog note correct: BubbleMock/live-badge live in PhoneShowcase (WEB-03), not Hero.",
    "p2_test_status": "pass",
    "p2_evidence": "Static read Hero.tsx:8-27 confirms min-h-dvh section, content bottom-aligned (justify-end), single tagline <p> 'meet dhizume — an ai assistant who can do real things.', a 'get started' Link -> /signin, an aria-hidden animated bounce scroll-cue chevron, all with 'rise' animation + staggered delays (120ms/240ms). Playwright landing.spec.ts:45 'primary get started CTA -> /signin' PASSED (covers the hero CTA reaching /signin).",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-03",
    "area": "web",
    "feature": "PhoneShowcase animated Telegram thread",
    "user_story": "As a visitor, I can watch a looping Telegram chat where dhizume takes a hard request, 'works', and replies with a file/table/checklist/card so that I believe she does real tasks, not just chat.",
    "expected_behavior": "Client component renders a phone with a thread that cycles scenarios via a step clock (advance + stepDelay): step 0 user bubble, 1 typing indicator with a work label, 2 reply text, 3 a rich artifact (FileChip/MiniTable/Checklist/InfoCard). Carries a 'live now' Badge and the 'she does the work' headline + CTAs. Reduced-motion users stay static on the first fully-revealed thread (FINAL_STEP). The conversation has role=img with a descriptive aria-label.",
    "source_refs": "web/components/marketing/PhoneShowcase.tsx:166-308; web/components/marketing/phoneScenarios.ts:21-165",
    "verify_method": "Playwright on local dev: load /, assert the 'live now' badge and headline render; emulate prefers-reduced-motion: reduce and assert the thread shows a full first scenario statically. Unit-test advance()/stepDelay()/FINAL_STEP via tsc/node import since phoneScenarios is framework-free.",
    "notes": "Replaces the checklist's BubbleMock + Capabilities bento + HowItWorks 3-step + Showcase marquee, none of which exist as separate components. The 'how it works' #how anchor is on this section | Section carries the #how anchor. The headline here is an <h1>; landing has exactly one h1 (other sections use h2), so no heading-level conflict.",
    "p2_test_status": "pass",
    "p2_evidence": "Unit test components/marketing/phoneScenarios.test.mts PASSED (node --test: 1 pass, 0 fail — validates advance()/stepDelay()/FINAL_STEP, framework-free). Static read PhoneShowcase.tsx:166-308 confirms: step clock cycles 0 user bubble / 1 typing indicator with work label / 2 reply / 3 RichBlock(FileChip/MiniTable/Checklist/InfoCard); 'live now' Badge (line 262-265) + 'she does the work' headline (266-270) + CTAs; reduced-motion branch (172-174) returns early so the thread stays static on FINAL_STEP; conversation has role=img + descriptive aria-label (211-213).",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-04",
    "area": "web",
    "feature": "Pricing TierGrid",
    "user_story": "As a visitor, I can compare free/pro/max plans with per-tier feature checklists so that I can pick the right amount of headroom before signing up.",
    "expected_behavior": "Renders one card per TIERS entry (free/pro/max) with price ('free' or $N/mo), quota ('N turns / mo'), blurb, a per-tier feature checklist (on=check, off=x), channel badges, and a 'get started' Button to /signin. The highlighted (pro) tier gets a lime ring and a 'most picked' badge and a primary CTA; others get secondary. Section id=pricing.",
    "source_refs": "web/components/marketing/TierGrid.tsx:71-158; web/lib/tiers.ts:17-43",
    "verify_method": "Playwright on local dev: load /#pricing, assert three cards, that pro shows 'most picked', and prices/quotas match lib/tiers (free 100, pro $19/1000, max $49/5000); assert each card CTA links to /signin.",
    "notes": "adversarial=real (cosmetic UX). FIX=use one noun ('messages') everywhere | FIXED: all 3 funnel surfaces say 'messages / month'; web build green.",
    "p2_test_status": "fail",
    "p2_evidence": "Static read TierGrid.tsx:71-158 + lib/tiers.ts:17-43: three cards (free/pro/max), pro highlighted with lime ring + 'most picked' badge + primary CTA (others secondary), per-tier feature checklist (on=check/off=x), channel badges, each CTA Link -> /signin, section id=pricing. Prices/quotas match catalog (free $0/100, pro $19/1000, max $49/5000). Playwright landing.spec.ts:12 'three tier labels' + :25 'both channels' PASSED. BUT TierGrid.tsx:101-102 renders quota as 'N turns / mo', while the wizard TierCard (ConnectWizard.tsx:221 'N messages / month') and dashboard (TierCard.tsx:80 / UsagePanel subtitle 'messages this period') say messages.",
    "error_found": "TierGrid.tsx:101 renders msgQuota as 'N turns / mo' while ConnectWizard.tsx:221 + TierCard.tsx:80 say 'N messages / month' — same number, two nouns across pricing->connect->dashboard funnel.",
    "severity": "low",
    "error_type": "ux",
    "p3_fix_status": "done",
    "p3_fix_ref": "web/components/marketing/TierGrid.tsx:101 'turns / mo'->'messages / month'",
    "p4_retest_status": "pass"
  },
  {
    "id": "WEB-05",
    "area": "web",
    "feature": "FAQ accordion (native details)",
    "user_story": "As a visitor, I can expand FAQ items to read answers so that I can resolve common doubts (real browser, privacy, cost) without leaving the page.",
    "expected_behavior": "Section id=faq renders 5 native <details>/<summary> items (zero JS, keyboard-accessible). The summary hides the default webkit marker and shows a chevron that rotates 45deg via group-open when expanded. Answers are in a <p> below each summary.",
    "source_refs": "web/components/marketing/Faq.tsx:32-63",
    "verify_method": "Playwright on local dev: load /#faq, click a summary and assert the answer becomes visible (details[open]); confirm keyboard Enter toggles it.",
    "notes": "",
    "p2_test_status": "pass",
    "p2_evidence": "Static read Faq.tsx:32-63: section id=faq, 5 native <details>/<summary> items (zero JS, keyboard-accessible by default), summary hides the webkit marker ([&::-webkit-details-marker]:hidden) and shows a chevron that rotates 45deg via group-open (group-open:rotate-45), answers in a <p> below each summary. Native <details> gets keyboard Enter toggle for free.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-06",
    "area": "web",
    "feature": "CtaBand closing call-to-action",
    "user_story": "As a visitor, I can see a final centered call-to-action so that I have one clear last chance to start before the footer.",
    "expected_behavior": "Full-width centered band over an aurora glow (aria-hidden) with a headline, sub-copy, and a single 'get started' primary Button linking to /signin. Wrapped in .reveal for scroll-in animation.",
    "source_refs": "web/components/marketing/CtaBand.tsx:7-28",
    "verify_method": "Playwright on local dev: scroll to the band, assert the headline and the get-started link to /signin are present.",
    "notes": "",
    "p2_test_status": "pass",
    "p2_evidence": "Static read CtaBand.tsx:7-28: full-width centered band over an aurora glow (aria-hidden), headline 'your assistant is one text away.', sub-copy, a single 'get started' primary Button Link -> /signin, wrapped in .reveal. Web build GREEN (renders on / route).",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-07",
    "area": "web",
    "feature": "Footer",
    "user_story": "As a visitor, I can use the footer to revisit sections or sign in so that I always have navigation at the bottom of the page.",
    "expected_behavior": "Server component: live Wordmark + tagline, an anchor cluster (#how, #pricing, #faq) plus a /signin Link, and a 'lowercase by design' signature.",
    "source_refs": "web/components/marketing/Footer.tsx:6-26",
    "verify_method": "Playwright on local dev / static read: assert footer contains the three anchors, a sign-in link to /signin, and the Wordmark.",
    "notes": "Footer sign-in link is the always-present sign-in affordance, but it is below the fold (see WEB-01).",
    "p2_test_status": "pass",
    "p2_evidence": "Static read Footer.tsx:6-26: server component with Wordmark + tagline 'a real assistant, in your chat.', anchor cluster #how/#pricing/#faq + a /signin Link, and the 'lowercase by design' signature.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-08",
    "area": "web",
    "feature": "VideoBackground (throne wallpaper)",
    "user_story": "As a visitor, I can see a looping ambient video behind the landing so that the brand feels alive while text stays readable.",
    "expected_behavior": "Pure-HTML, no-JS, aria-hidden fixed full-bleed background: a desktop 16:9 <video> (hidden sm:block) and a mobile 9:16 <video> (block sm:hidden), both autoPlay/muted/loop/playsInline with poster fallbacks, over a dark scrim for WCAG contrast. Reduced-motion users see the static poster (videos hidden via CSS).",
    "source_refs": "web/components/marketing/VideoBackground.tsx:8-39; web/app/page.tsx:14-31",
    "verify_method": "Static read + Playwright on local dev: assert two <video> elements with the correct responsive classes, poster attrs, and the scrim div; confirm only landing renders it (dashboard/connect/signin do not).",
    "notes": "Not in checklist (checklist had it bundled under Hero/BubbleMock); it is a distinct, real component | Confirmed distinct, real component; only rendered on /.",
    "p2_test_status": "pass",
    "p2_evidence": "Static read VideoBackground.tsx:8-39: aria-hidden fixed full-bleed bg, desktop 16:9 <video class='hidden sm:block'> and mobile 9:16 <video class='block sm:hidden'>, both autoPlay/muted/loop/playsInline with poster fallbacks, plus a dark scrim div. page.tsx:14-31 renders VideoBackground only on the landing (dashboard/connect/signin pages do not import it). Reduced-motion poster fallback is handled in CSS (.video-bg-media hidden).",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-09",
    "area": "web",
    "feature": "RevealObserver scroll-in",
    "user_story": "As a visitor, I can have sections fade in as I scroll so that the page feels crafted, while reduced-motion and no-JS users still see all content.",
    "expected_behavior": "Client effect adds 'reveal-in' to .reveal elements once they intersect the viewport (then unobserves), via IntersectionObserver (rootMargin -8% bottom, threshold 0.12). Falls back to showing all immediately when prefers-reduced-motion or IntersectionObserver is unavailable. A MutationObserver re-scans on client navigations so wizard/dashboard reveals work. A <noscript> style in layout forces .reveal visible without JS.",
    "source_refs": "web/components/RevealObserver.tsx:9-52; web/app/layout.tsx:41-51",
    "verify_method": "Playwright on local dev: assert .reveal elements gain reveal-in on scroll; emulate reduced-motion and assert all .reveal are shown immediately. Static read confirms the noscript fallback.",
    "notes": "",
    "p2_test_status": "pass",
    "p2_evidence": "Static read RevealObserver.tsx:9-52: adds 'reveal-in' to .reveal once they intersect then unobserves (IntersectionObserver rootMargin '0px 0px -8% 0px', threshold 0.12); falls back to showAll() when prefers-reduced-motion or IntersectionObserver missing; a MutationObserver re-scans on DOM mutations (client nav). layout.tsx:41-43 has the <noscript> style forcing .reveal visible without JS. Web build GREEN.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-10",
    "area": "web",
    "feature": "Wordmark (pulsing dot)",
    "user_story": "As a visitor, I can see the dhizume wordmark with a living pulse so that the brand reads as an always-on assistant and I can click back home.",
    "expected_behavior": "Link (default href=/) with the masked avatar in a ring plus an animated rose dot (ping-ring + pulse-dot, aria-hidden) and the lowercase mono name. Used in Nav and Footer.",
    "source_refs": "web/components/marketing/Wordmark.tsx:8-41",
    "verify_method": "Static read + Playwright: assert the Wordmark links to '/' and renders the avatar image and pulse dot.",
    "notes": "",
    "p2_test_status": "pass",
    "p2_evidence": "Static read Wordmark.tsx:8-41: Link with default href='/', masked avatar Image in a ring plus an animated rose dot (ping-ring + pulse-dot, aria-hidden) and the lowercase mono name 'dhizume'. Imported by Nav.tsx and Footer.tsx. Web tsc+build GREEN.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-11",
    "area": "web",
    "feature": "Sign in / sign up card (email + password)",
    "user_story": "As a user, I can sign in or create an account with email and password so that I can reach my dashboard or onboarding.",
    "expected_behavior": "Tablist toggles flow signIn/signUp (clears error on switch). On submit calls signIn('password', {email,password,flow}); signUp enforces MIN_PASSWORD=8 client-side (inline 'at least 8 characters' hint turns danger; input minLength on signup) and routes to /connect, signIn routes to /dashboard. Submit is disabled while pending or when email/password empty, with 'creating…'/'signing in…' labels. Any thrown error is replaced with a friendly lowercase message (never the raw Convex/InvalidAccountId stack).",
    "source_refs": "web/components/auth/SignInCard.tsx:24-173",
    "verify_method": "Playwright on local dev + dev Convex with a throwaway tenant: sign up a fresh email+pw and assert redirect to /connect; sign in an existing user and assert /dashboard; submit a short password on signup and assert the inline hint + blocked submit; submit bad creds and assert a friendly error containing no 'server error'/'node_modules'/'.ts:'.",
    "notes": "Checklist omitted that signUp redirects to /connect while signIn redirects to /dashboard (not a single '/connect' redirect) | The redirect split (signUp->/connect, signIn->/dashboard) is INTENTIONAL and documented in-code (lines 53-54): new users connect a channel first, returning users go to the dashboard. Not a bug. Full happy/failure-path Playwright would need a throwaway dev-Convex tenant (the catalog's verify_method), but the behavior is statically correct and the error-masking matches the prior gotcha fix.",
    "p2_test_status": "pass",
    "p2_evidence": "Static read SignInCard.tsx:24-173: role=tablist toggles signIn/signUp (switchFlow clears error); handlePassword calls signIn('password',{email,password,flow}); signUp enforces MIN_PASSWORD=8 client-side (returns early with inline message; the 'at least 8 characters' hint turns danger when tooShort; input minLength on signup at 136); routes signUp->/connect, signIn->/dashboard (line 55); submit disabled while pwPending or email/password empty (163) with 'creating…'/'signing in…' labels (165-171); catch{} replaces any thrown error with a friendly lowercase message (60-64), never the raw stack. Tsc+build GREEN.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-12",
    "area": "web",
    "feature": "Google OAuth sign-in",
    "user_story": "As a user, I can continue with Google so that I can authenticate without a password.",
    "expected_behavior": "A 'continue with google' secondary Button (inline Google 'G' SVG, no dep) calls signIn('google', {redirectTo:'/dashboard'}), which navigates the whole page to Google then back. Disabled while pwPending or googlePending; shows 'redirecting…'. On synchronous failure it re-enables and shows a friendly error.",
    "source_refs": "web/components/auth/SignInCard.tsx:70-83;web/components/auth/SignInCard.tsx:182-208",
    "verify_method": "Playwright on local dev: click 'continue with google' and assert the navigation begins toward accounts.google.com with the configured client_id/redirect_uri (do not complete a real Google login); assert button shows 'redirecting…' and is disabled. Static read confirms inline SVG (no external dep).",
    "notes": "Not in the checklist at all — Google OAuth was added 2026-06-22 per the file comment. Email-code (ResendOTP) is intentionally hidden until AUTH_RESEND_KEY lands | Google -> /dashboard is intentional and consistent with signIn (returning-user) redirect; documented at lines 76-77. A live OAuth navigation assertion is out of scope (would hit accounts.google.com); inline-SVG/no-dep and disabled/label states are statically confirmed.",
    "p2_test_status": "pass",
    "p2_evidence": "Static read SignInCard.tsx:70-83 + 182-208: 'continue with google' secondary Button with an inline Google 'G' SVG (GoogleIcon, no external dep); handleGoogle calls signIn('google',{redirectTo:'/dashboard'}); disabled while googlePending||pwPending; shows 'redirecting…' while pending; on synchronous throw it re-enables (setGooglePending(false)) and shows a friendly error. Tsc+build GREEN.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-13",
    "area": "web",
    "feature": "Sign-in page layout + metadata",
    "user_story": "As a user, I can land on a focused sign-in page so that I authenticate without marketing distractions, and search/social previews show the right title.",
    "expected_behavior": "Server page sets metadata.title 'sign in or sign up · dhizume', renders a centered max-w-md main (id=main) with decorative grid-lines + aurora backdrops (aria-hidden), a decorative aria-hidden live wordmark, an h1 'sign in or sign up', and the SignInCard.",
    "source_refs": "web/app/signin/page.tsx:4-36",
    "verify_method": "Playwright on local dev: load /signin, assert document title and a single h1 'sign in or sign up'; assert the SignInCard is present. Or static read.",
    "notes": "",
    "p2_test_status": "pass",
    "p2_evidence": "Static read signin/page.tsx:4-36: metadata.title 'sign in or sign up · dhizume'; centered max-w-md main id=main; decorative grid-lines + aurora aria-hidden backdrops; an aria-hidden decorative live wordmark (with ping-ring/pulse-dot); a single h1 'sign in or sign up'; and <SignInCard/>. Web build GREEN (route /signin present).",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-14",
    "area": "web",
    "feature": "Connect wizard state machine",
    "user_story": "As a new user, I can step through tier → channel → pair → done so that I onboard one decision at a time and see when my channel connects.",
    "expected_behavior": "Client state machine with steps tier/channel/pair/done and StepDots progress indicator. Subscribes to api.app.channels.myChannels; when the chosen channel appears verified it flips to 'done' (no polling). A persistent role=status aria-live=polite region wraps pair+done; on flip to done it focuses the done heading (tabIndex=-1). initialStep prop (from /connect?step=channel) lets onboarded users skip the tier step. Per-step errors render in a role=alert.",
    "source_refs": "web/components/connect/ConnectWizard.tsx:56-301; web/app/connect/page.tsx:9-21",
    "verify_method": "Playwright on local dev + dev Convex throwaway tenant: drive tier→channel→pair, then simulate the binding via a test seam / mutation and assert the wizard reactively flips to 'done' and moves focus to the heading; assert StepDots advance and ?step=channel starts at the channel step.",
    "notes": "blocked: test seam disabled (ALLOW_TEST_SEED/WORKER_SECRET unset on dev:zany-tapir-501). The reactive-subscription invariant is statically sound (useEffect keyed on [step,channel,myChannels], no setInterval) and the StepDots/initialStep wiring is type-checked GREEN; the live flip is exercised by connect-telegram.spec.ts only when the seam is enabled.",
    "p2_test_status": "blocked",
    "p2_evidence": "Static read ConnectWizard.tsx:56-301 confirms the state machine (tier/channel/pair/done), StepDots, the myChannels subscription driving the pair->done flip via useEffect (81-87, NO polling), focus-move to the done heading (91-95), initialStep prop (connect/page.tsx passes ?step=channel), and per-step role=alert errors. BUT the reactive-flip e2e (connect-telegram.spec.ts) requires the double-gated testing:testRedeemTelegram wrapper (test.skip when WORKER_SECRET absent) — disabled because ALLOW_TEST_SEED is unset on the live dev deployment; we must not enable it.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-15",
    "area": "web",
    "feature": "Tier-selection step (chooseTier)",
    "user_story": "As a new user, I can pick a plan so that my account is provisioned before I connect a channel.",
    "expected_behavior": "Renders a Card button per TIERS entry (label, price, quota 'N messages / month', 'popular' badge on highlight). Clicking calls chooseTier({tier}); paid tiers return granted:false with a coming-soon message, which is stashed as tierMessage and surfaced on the channel step, then the wizard advances to 'channel' regardless. Buttons disable while choosing (shows '· setting up…'); failure shows an error.",
    "source_refs": "web/components/connect/ConnectWizard.tsx:97-111;web/components/connect/ConnectWizard.tsx:193-228",
    "verify_method": "Playwright on local dev + dev Convex: choose 'free' and assert advance to channel; choose 'pro' and assert it still advances and the coming-soon tierMessage shows on the channel step (paid not granted).",
    "notes": "Uses 'N messages / month' (vs marketing TierGrid 'turns / mo' — see WEB-04). Full paid-tier-advance e2e needs a dev-Convex tenant, but the advance-regardless + tierMessage-stash logic is statically correct.",
    "p2_test_status": "pass",
    "p2_evidence": "Static read ConnectWizard.tsx:97-111 + 193-228: a Card button per TIERS entry (label, price 'free' or '$N/mo', quota 'N messages / month', 'popular' badge on highlight); onChooseTier calls chooseTier({tier}); res.granted false -> message stashed as tierMessage and surfaced on the channel step (232-239); advances to 'channel' regardless (105); buttons disable while choosing (199) showing '· setting up…' (222); failure sets a role=alert error. Tsc+build GREEN.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-16",
    "area": "web",
    "feature": "Channel choice (imessage/telegram)",
    "user_story": "As a user, I can choose iMessage or Telegram so that I pair the channel I actually use.",
    "expected_behavior": "Two equal Card buttons (imessage/telegram) each with aria-label 'connect <title>'. Picking one calls createPairingToken({channel}) in the parent, which mints {token,code,expiresAt,deepLink} and advances to pair. While minting, the picked card shows 'minting your link…' (in a polite sr-only live region and a visible aria-hidden hint) and both buttons disable.",
    "source_refs": "web/components/connect/ChannelChoice.tsx:25-75;web/components/connect/ConnectWizard.tsx:113-131",
    "verify_method": "Playwright on local dev + dev Convex: at the channel step click telegram and assert it mints a token (pairing panel appears) and the cards disabled during minting.",
    "notes": "Token-mint round-trip e2e needs a dev-Convex tenant; the disable/announce wiring is statically correct.",
    "p2_test_status": "pass",
    "p2_evidence": "Static read ChannelChoice.tsx:25-75 + ConnectWizard.tsx:113-131: two equal Card buttons (imessage/telegram) each aria-label 'connect <title>'; onPick -> parent onPickChannel calls createPairingToken({channel}) returning {token,code,expiresAt,deepLink} and setStep('pair'); while minting the picked card shows 'minting your link…' in an sr-only polite live region (63-65) and a visible aria-hidden hint (66-68), and both buttons disable (pending!==null -> disabled). Tsc+build GREEN.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-17",
    "area": "web",
    "feature": "Pairing panel (Telegram + iMessage + expiry/renew)",
    "user_story": "As a user, I can open my channel prefilled or copy the pair code, and refresh an expired link, so that I can complete pairing reliably from any device.",
    "expected_behavior": "Telegram branch: a Qr plus an <a> 'open telegram' (deepLink, else built from TELEGRAM_BOT_USERNAME, else a manual '/start <token>' instruction) and a 'waiting for you to tap start…' row. iMessage branch: 'open messages' <a> (imessagePairLink) + 'send as sms instead' (smsPairLink) when IMESSAGE_NUMBER is set (else a plain 'text pair <code>' instruction), a copyable 'pair <code>' chip writing to clipboard with copied/failed states (announced + visible alert on failure), and a 'waiting for your message…' row. A footer shows the Countdown + an inline 'new link' renew. When expired it swaps to a 'this link expired' block with a 'get a new link' button calling onRenew.",
    "source_refs": "web/components/connect/PairingPanel.tsx:44-206",
    "verify_method": "Playwright on local dev + dev Convex: at pair step assert the channel-appropriate open link (href starts imessage:// or t.me); click copy and assert 'copied' (grant clipboard); set a short-lived token and assert the expired block + renew re-mints.",
    "notes": "Channel-appropriate links + expiry/renew fully wired. Clipboard/expired e2e needs a dev tenant + a short-lived token; behavior is statically correct.",
    "p2_test_status": "pass",
    "p2_evidence": "Static read PairingPanel.tsx:44-206: telegram branch shows Qr + an <a data-testid=tg-open> (deepLink ?? built from TELEGRAM_BOT_USERNAME ?? a manual '/start <token>' block) and a 'waiting for you to tap start…' WaitingRow; iMessage branch shows 'open messages' <a> (imessagePairLink) + 'send as sms instead' <a> (smsPairLink) when IMESSAGE_NUMBER set, else a plain 'text pair <code>' instruction, a copyable 'pair <code>' chip writing to clipboard with copied/failed states (announced in an sr-only role=status + a visible role=alert on failure), and a 'waiting for your message…' row; a footer with Countdown + inline 'new link' renew; expired state swaps to a 'this link expired' block with a 'get a new link' onRenew button (90-96). Tsc+build GREEN.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-18",
    "area": "web",
    "feature": "Client-side QR code",
    "user_story": "As a user pairing on another device, I can scan a QR code so that I don't have to type the link, while keeping my pairing credential off third-party servers.",
    "expected_behavior": "Renders the value to a <canvas> via the qrcode npm package entirely in the browser (no third-party host). Canvas has role=img + an actionable aria-label. On generation failure it shows a fallback panel with the raw value as selectable text; renders nothing when value is empty.",
    "source_refs": "web/components/connect/Qr.tsx:16-69",
    "verify_method": "Playwright on local dev: at the telegram pair step assert a canvas[role=img] with the aria-label; static read confirms encoding is client-side only. Network-tab assertion that no QR value is sent to any external host.",
    "notes": "Client-side-only encoding confirmed by code (no fetch); a network-tab assertion is unnecessary given the import is the qrcode lib used in-browser.",
    "p2_test_status": "pass",
    "p2_evidence": "Static read Qr.tsx:16-69: encodes value to a <canvas> via the qrcode npm package entirely client-side (QRCode.toCanvas in a useEffect — no network/third-party host); canvas has role='img' + an actionable aria-label; on .catch it shows a fallback panel with the raw value as selectable text (data-testid=qr-fallback); returns null when value is empty (line 40). Tsc+build GREEN.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-19",
    "area": "web",
    "feature": "Pairing countdown with SR thresholds",
    "user_story": "As a user, I can see how long my pairing link is valid and hear coarse time milestones so that I act before it expires without screen-reader spam.",
    "expected_behavior": "Display-only clock ticks once/sec via setInterval (not data polling). Visible label 'expires in <m s>' (aria-hidden) turns danger / 'link expired' at/after expiry. A polite sr-only region announces only at thresholds 60s/30s/10s (each once) and on expiry; threshold tracking resets when expiresAt changes.",
    "source_refs": "web/components/connect/Countdown.tsx:13-80;web/lib/utils.ts:7-14",
    "verify_method": "Static read + Playwright on local dev: mint a token with a near expiry and assert the visible countdown decrements and flips to 'link expired'; assert the sr-only region only updates at thresholds (not every second).",
    "notes": "",
    "p2_test_status": "pass",
    "p2_evidence": "Static read Countdown.tsx:13-80 + lib/utils.ts:7-14: display-only clock ticks once/sec via setInterval (re-renders the label, NOT data polling — comment line 10-12); visible 'expires in <m s>' label is aria-hidden (71-77) and turns danger / 'link expired' at/after expiry; a polite sr-only region announces only at 60s/30s/10s thresholds (ANNOUNCE_THRESHOLDS_MS, each once via announcedRef) and on expiry (37-41); threshold tracking + prevExpired reset when expiresAt changes (21-26). Tsc+build GREEN.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-20",
    "area": "web",
    "feature": "Pick-a-different-channel + back nav",
    "user_story": "As a user mid-pairing, I can go back to pick a different channel or return to the dashboard so that I'm never stuck.",
    "expected_behavior": "On the pair step a '← pick a different channel' button calls startOver (clears channel/minted/error, returns to 'channel'). The wizard header has a '← back' Link to /dashboard. The done step offers a 'go to dashboard' Button.",
    "source_refs": "web/components/connect/ConnectWizard.tsx:152-157;web/components/connect/ConnectWizard.tsx:176-185;web/components/connect/ConnectWizard.tsx:264-296",
    "verify_method": "Playwright on local dev: at the pair step click 'pick a different channel' and assert return to channel step; assert the header back link and done 'go to dashboard' both target /dashboard.",
    "notes": "",
    "p2_test_status": "pass",
    "p2_evidence": "Static read ConnectWizard.tsx:152-157 (startOver clears channel/minted/error -> setStep('channel')) wired to the pair-step '← pick a different channel' button (264-271); the header '← back' Link -> /dashboard (176-181); the done step 'go to dashboard' Button Link -> /dashboard (291-295). Tsc+build GREEN.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-21",
    "area": "web",
    "feature": "Dashboard shell (auth-gated, skeleton)",
    "user_story": "As a signed-in user, I can open a dashboard that shows my plan, usage, and channels so that I can manage my assistant.",
    "expected_behavior": "Client shell with a TopBar; an AuthLoading state shows a polite 'loading dashboard…' region + an aria-hidden three-card skeleton; the Authenticated state renders an sr-only h1, a 'dashboard loaded' polite region, and a responsive 1→2→3 column grid of TierCard, UsagePanel, ConnectionsPanel. The page itself is protected by middleware. Server page just renders the shell.",
    "source_refs": "web/components/dashboard/DashboardShell.tsx:10-43;web/app/dashboard/page.tsx:5-7",
    "verify_method": "Playwright on local dev + dev Convex: visit /dashboard while signed in and assert the three panels render and exactly one h1; verify the skeleton/loading region appears during auth resolution.",
    "notes": "blocked: signed-in panel-render verification needs the disabled seed seam (ALLOW_TEST_SEED unset). The shell structure (single h1, AuthLoading skeleton, Authenticated grid) is statically correct and tsc/build GREEN; route protection covered by auth-gate.spec.ts.",
    "p2_test_status": "blocked",
    "p2_evidence": "Static read DashboardShell.tsx:10-43 + dashboard/page.tsx:5-7: TopBar; AuthLoading shows an sr-only polite 'loading dashboard…' region + an aria-hidden 3-card skeleton; Authenticated renders an sr-only h1 'dashboard', a 'dashboard loaded' polite region, and a 1->2->3 col grid of TierCard/UsagePanel/ConnectionsPanel; server page just renders the shell. auth-gate.spec.ts:9 (/dashboard -> /signin) PASSED (middleware protection confirmed). BUT asserting the three panels render with live data while signed in needs a dev-Convex authenticated tenant (the testing:* seam is disabled).",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-22",
    "area": "web",
    "feature": "TopBar + user email",
    "user_story": "As a signed-in user, I can see my account identity in the top bar so that I know which account I'm in.",
    "expected_behavior": "TopBar shows the 'dhizume / dashboard' brand and, only while Authenticated, a UserEmail that queries api.app.account.currentUser: shows a pulse skeleton while undefined, then email ?? displayName ?? name (truncated, sm+), or nothing if no label. Mounting only under Authenticated avoids firing the gated query unauthenticated during sign-out.",
    "source_refs": "web/components/dashboard/DashboardShell.tsx:45-91",
    "verify_method": "Playwright on local dev + dev Convex: sign in with a known email and assert it appears in the top bar; sign out and assert no uncaught 'unauthenticated' error fires.",
    "notes": "blocked: needs an authenticated dev-Convex session (seam disabled). The Authenticated-gated mount that prevents the uncaught 'unauthenticated' error on sign-out is statically correct and matches the documented design.",
    "p2_test_status": "blocked",
    "p2_evidence": "Static read DashboardShell.tsx:45-91: TopBar shows the 'dhizume / dashboard' brand and, only inside <Authenticated>, <UserEmail/> which queries api.app.account.currentUser — pulse skeleton while undefined, then email ?? displayName ?? name (truncated, sm+), or null if no label; mounting UserEmail only under Authenticated avoids firing the gated query unauthenticated during sign-out (comment 59-61). BUT asserting the email renders for a known signed-in user needs a dev-Convex authenticated tenant.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-23",
    "area": "web",
    "feature": "Tier card + upgrade/checkout",
    "user_story": "As a signed-in user, I can see my current plan and start an upgrade so that I can increase my headroom.",
    "expected_behavior": "Reads currentUser + myUsage; current tier = usage.tier ?? user.tier ?? 'free'. Operators get a 'max · unlimited' badge and an 'everything's unlocked' note (no upgrade buttons). Others get upgrade Buttons only for tiers strictly above current (higherTiers). Clicking calls startCheckout({tier}); if it returns an http checkoutUrl it redirects, otherwise (stub v1) it shows a coming-soon banner; failure shows a retry banner. Buttons disable while a checkout is pending ('starting…').",
    "source_refs": "web/components/dashboard/TierCard.tsx:28-154",
    "verify_method": "Playwright on local dev + dev Convex: as a free user assert 'upgrade to pro/max' buttons; click and assert the coming-soon banner (stub returns no http url); as an operator assert the unlimited badge and no upgrade buttons.",
    "notes": "blocked: needs an authenticated dev-Convex session (seam disabled). The higherTiers/operator/stub-banner logic is statically correct and tsc/build GREEN. Uses 'messages / month' in the subtitle (consistent with the wizard/dashboard, vs marketing 'turns' — WEB-04).",
    "p2_test_status": "blocked",
    "p2_evidence": "Static read TierCard.tsx:28-154: reads currentUser+myUsage; currentTier = usage.tier ?? user.tier ?? 'free'; operators get a 'max · unlimited' badge + 'everything's unlocked' note and NO upgrade buttons (87-90); others get upgrade Buttons only for higherTiers (strictly above current, 21-26); onUpgrade calls startCheckout({tier}) -> redirect if checkoutUrl startsWith 'http' else show a coming-soon banner (57-61); failure shows a retry banner (62); buttons disable while pending ('starting…', 144). BUT operator-vs-free and the checkout-stub banner need a dev-Convex authenticated tenant.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-24",
    "area": "web",
    "feature": "Usage panel (quota meter)",
    "user_story": "As a signed-in user, I can see how many messages I've used this period and when it resets so that I can pace my usage.",
    "expected_behavior": "Reads api.app.usage.myUsage. undefined → aria-hidden skeleton; null → 'no plan yet — pick a tier'. Unlimited plans render a decorative full bar + 'unlimited' text with a reset date (no progressbar role, to avoid an indeterminate meter). Quota plans render a role=progressbar (aria-valuenow/min/max/valuetext) whose fill width is the used/quota percentage and turns danger ('low') when remaining<=0 or <=10% of quota; shows 'used / quota', 'N left', and 'resets <date>'. Divide-by-zero guarded when quota is 0.",
    "source_refs": "web/components/dashboard/UsagePanel.tsx:16-122",
    "verify_method": "Playwright on local dev + dev Convex: seed a tenant near quota and assert the progressbar aria values + 'left' count + danger color at <=10%; seed an unlimited/operator tenant and assert the 'unlimited' text without a progressbar role.",
    "notes": "blocked: aria-value/danger-at-10% / unlimited-no-progressbar assertions need a seeded dev tenant (seam disabled). The meter math + role gating is statically correct and tsc/build GREEN.",
    "p2_test_status": "blocked",
    "p2_evidence": "Static read UsagePanel.tsx:16-122: reads api.app.usage.myUsage; undefined -> aria-hidden skeleton; null -> 'no plan yet — pick a tier'; unlimited -> a decorative full bar (aria-hidden) + 'unlimited' text + reset date, NO progressbar role (avoids an indeterminate meter, comment 48-49); quota plans -> role=progressbar with aria-valuenow/min/max/valuetext, fill width = used/quota %, danger ('low') when remaining<=0 or <=10% of quota (83), shows 'used / quota', 'N left', 'resets <date>'; divide-by-zero guarded when quota 0 (81). BUT seeding a near-quota / unlimited tenant needs the disabled seam.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-25",
    "area": "web",
    "feature": "Connections panel (list/disconnect/empty)",
    "user_story": "As a signed-in user, I can see my connected channels with masked addresses and disconnect them so that I control where my assistant reaches me.",
    "expected_behavior": "Reads api.app.channels.myChannels. undefined → polite 'loading channels…' + aria-hidden skeleton rows. Empty → an empty-state with a 'connect a channel' link to /connect?step=channel. Non-empty → a list of rows: a StatusDot (verified vs pending), the channel label, a masked address ('••• ' + last 4) with a '· pending' tag when unverified, an accessible row aria-label 'imessage ending in NNNN', and a 'disconnect' Button calling disconnectChannel({channel,address}) (reactive removal; success/failure announced in a polite region, 'disconnecting…' pending label). A 'connect another' link shows in the header when channels exist.",
    "source_refs": "web/components/dashboard/ConnectionsPanel.tsx:26-167",
    "verify_method": "Playwright on local dev + dev Convex: with no channels assert the empty-state link to /connect?step=channel; with a bound channel assert the masked tail + pending dot, click disconnect and assert the row reactively disappears and a polite announcement fires.",
    "notes": "blocked: bound-channel render + reactive disconnect need the disabled seed seam (ALLOW_TEST_SEED unset; isolation.spec relies on it). The empty-state link and masking/labelling logic are statically correct; multi-tenant isolation invariant is covered by the offline docker isolation smoke (alice->alice, bob->bob, no cross-tenant) + fleet controller suite.",
    "p2_test_status": "blocked",
    "p2_evidence": "Static read ConnectionsPanel.tsx:26-167: reads api.app.channels.myChannels; undefined -> polite 'loading channels…' + aria-hidden skeleton rows; empty -> empty-state with a 'connect a channel' Link to /connect?step=channel (94-99); non-empty -> rows with StatusDot (verified vs pending), label, masked address ('••• ' + last4) with a '· pending' tag when unverified, an accessible li aria-label 'imessage ending in NNNN' (137), and a 'disconnect' Button calling disconnectChannel({channel,address}) with success/failure announced in a polite region and a 'disconnecting…' pending label; a 'connect another' header link when channels exist (35-43). BUT the bound-channel / reactive-disconnect path needs a seeded dev tenant. isolation.spec.ts (the cross-tenant render check) is gated by the disabled testing:* seam.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-26",
    "area": "web",
    "feature": "Sign-out button",
    "user_story": "As a signed-in user, I can sign out so that my session ends and I'm returned to the public landing.",
    "expected_behavior": "Ghost Button (aria-label 'sign out') calls signOut() then hard-navigates to '/' (window.location.href, not router.push) so the dashboard tears down before any gated query re-fires; on failure it re-enables for retry. Shows 'signing out…' while pending.",
    "source_refs": "web/components/dashboard/SignOutButton.tsx:9-38",
    "verify_method": "Playwright on local dev + dev Convex: sign in, click sign out, assert navigation to '/' and that protected routes now redirect to /signin.",
    "notes": "The full sign-in->sign-out->/ navigation e2e would need a dev tenant, but the hard-redirect-on-signout pattern (the fix for the gotcha about gated queries firing mid-transition) is statically correct, and the gated-routes-redirect side is covered GREEN by auth-gate.spec.ts.",
    "p2_test_status": "pass",
    "p2_evidence": "Static read SignOutButton.tsx:9-38: ghost Button aria-label 'sign out' calls signOut() then hard-navigates window.location.href='/' (not router.push) so the dashboard tears down before any gated query re-fires (comment 17-19); on failure re-enables (setPending(false)); shows 'signing out…' while pending. auth-gate.spec.ts confirms protected routes redirect to /signin when unauthenticated (the post-sign-out state). Tsc+build GREEN.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-27",
    "area": "web",
    "feature": "Dashboard StatCard chrome",
    "user_story": "As a developer, I can use one shared card shell so that the plan/usage/channels cards stay symmetric, equal-height, and consistently styled.",
    "expected_behavior": "Shared StatCard (icon box + ring, title via CardTitle as=h2, optional headerRight slot, optional subtitle, body) with a rose radial glow that intensifies on hover, h-full flex layout, and a hover lift. Exports inline Lucide-style PlanIcon/UsageIcon/ChannelsIcon (no icon dependency). Spreads extra HTML attrs (e.g. data-testid).",
    "source_refs": "web/components/dashboard/StatCard.tsx:22-109",
    "verify_method": "npx tsc --noEmit for type correctness; static read confirms h2 heading level and icon SVGs are inline (no dep).",
    "notes": "Not in checklist; it is the shared chrome behind TierCard/UsagePanel/ConnectionsPanel | h2 heading level confirmed (CardTitle as='h2' at line 56), under each panel's sr-only page h1.",
    "p2_test_status": "pass",
    "p2_evidence": "Static read StatCard.tsx:22-109: shared StatCard (icon box + ring, title via CardTitle as='h2', optional headerRight slot, optional subtitle, body), a rose radial glow that intensifies on group-hover (opacity-40 -> group-hover:opacity-80), h-full flex layout + hover lift; inline Lucide-style PlanIcon/UsageIcon/ChannelsIcon (no icon dependency); spreads ...props (extra HTML attrs e.g. data-testid). Web tsc GREEN.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-28",
    "area": "web",
    "feature": "Button primitive",
    "user_story": "As a developer, I can use a Button with consistent variants/sizes and a visible focus ring so that all actions look and behave the same and are keyboard-accessible.",
    "expected_behavior": "forwardRef <button> with variants primary/secondary/ghost/danger and sizes sm/md/lg, a focus-visible lime ring (with offset), disabled opacity + pointer-events-none, and motivated hover/press micro-motion. Defaults variant=primary, size=md; merges className.",
    "source_refs": "web/components/ui/button.tsx:14-47",
    "verify_method": "npx tsc --noEmit; Playwright on local dev: tab to a Button and assert the focus-visible ring; assert a disabled Button is non-interactive.",
    "notes": "",
    "p2_test_status": "pass",
    "p2_evidence": "Static read button.tsx:14-47: forwardRef <button> with variants primary/secondary/ghost/danger and sizes sm/md/lg, a focus-visible lime ring with offset (focus-visible:ring-2 focus-visible:ring-lime focus-visible:ring-offset-2), disabled opacity-50 + pointer-events-none, hover/press micro-motion (hover:-translate-y-px active:translate-y-0); defaults variant=primary size=md; merges className via cn. Web tsc+build GREEN.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-29",
    "area": "web",
    "feature": "Card / CardTitle / CardDescription",
    "user_story": "As a developer, I can use card primitives with a controllable heading level so that surfaces are consistent and the document heading hierarchy stays correct.",
    "expected_behavior": "Card is a bordered surface div with an inner top-highlight shadow. CardTitle is a forwardRef heading whose tag is set by `as` (default h3; pass as='h2' under a page h1) so callers can focus it programmatically. CardDescription is a muted <p>. All merge className.",
    "source_refs": "web/components/ui/card.tsx:6-51",
    "verify_method": "npx tsc --noEmit; static read confirms `as` controls the heading tag and CardTitle forwards a ref.",
    "notes": "CardTitle forwardRef confirmed (used by ConnectWizard doneHeadingRef for programmatic focus).",
    "p2_test_status": "pass",
    "p2_evidence": "Static read card.tsx:6-51: Card is a bordered surface div with an inner top-highlight shadow; CardTitle is a forwardRef heading whose tag is set by `as` (default h3; callers pass as='h2'); CardDescription is a muted <p>; all merge className via cn. Web tsc GREEN.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-30",
    "area": "web",
    "feature": "Badge + StatusDot",
    "user_story": "As a developer, I can use badges and a live connection dot so that plan/channel state reads consistently across pages.",
    "expected_behavior": "Badge is a rounded mono pill with tones neutral/lime/muted, merging className. StatusDot is an aria-hidden dot that is solid lime when connected and a pulsing faint dot while not connected (waiting).",
    "source_refs": "web/components/ui/badge.tsx:12-40",
    "verify_method": "npx tsc --noEmit; static read confirms tones map and the connected/pulsing dot states.",
    "notes": "",
    "p2_test_status": "pass",
    "p2_evidence": "Static read badge.tsx:12-40: Badge is a rounded mono pill with tones neutral/lime/muted, merging className; StatusDot is an aria-hidden dot that is solid lime when connected and bg-faint + pulse-dot (pulsing faint) while not connected. Web tsc GREEN.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-31",
    "area": "web",
    "feature": "Tier catalog (lib/tiers.ts)",
    "user_story": "As a developer, I can read tier names/prices/quotas/channels from one client catalog so that marketing and onboarding render consistent plan data without a server round-trip.",
    "expected_behavior": "Exports TierName ('free'|'pro'|'max'), TierSpec, and TIERS: free ($0/100, both channels), pro ($19/1000, highlight), max ($49/5000), each with both imessage+telegram channels and a blurb. Documented to mirror the control-plane billing/tiers catalog.",
    "source_refs": "web/lib/tiers.ts:5-43",
    "verify_method": "Static read / unit assertion via tsc: confirm the three tiers and their price/quota values match the server catalog comment (free 100, pro 1000@$19, max 5000@$49).",
    "notes": "adversarial=real (maintainability). FIX=add a drift check (test) or single shared source | FIXED: drift guard added + green; catalogs agree field-by-field.",
    "p2_test_status": "fail",
    "p2_evidence": "Read web/lib/tiers.ts:5-43 vs control-plane/convex/billing/tiers.ts:14-51. The values AGREE NOW: free $0/100, pro $19/1000 (highlight), max $49/5000, all with channels [imessage,telegram] and identical blurbs. So no value divergence. BUT lib/tiers.ts is a hand-maintained MIRROR (its header comment 1-4 says 'keep this in sync with the server catalog') with NO automated drift guard — nothing fails if the two diverge in future. Marking fail (logical, low) per the catalog authors' missing-guard note.",
    "error_found": "web/lib/tiers.ts:17-43 is a hand-maintained mirror of control-plane/convex/billing/tiers.ts:26-51; field-by-field identical NOW but no drift guard (no cross-import/test/build assert) so a future edit silently desyncs.",
    "severity": "low",
    "error_type": "logical",
    "p3_fix_status": "done",
    "p3_fix_ref": "lab/tests/test_tiers_drift.py (parses both tiers.ts, asserts agreement; runs in lab gate)",
    "p4_retest_status": "pass"
  },
  {
    "id": "WEB-32",
    "area": "web",
    "feature": "Deep-link builders + public env",
    "user_story": "As a developer, I can build Telegram/iMessage/SMS pairing links from a public env config so that the wizard opens the right app prefilled with the pair code.",
    "expected_behavior": "telegramDeepLink(botUsername, token) → https://t.me/<bot>?start=<token>; imessagePairLink(number, code) → imessage://<number>?body=pair%20<code>; smsPairLink(number, code) → sms:<number>?body=pair%20<code> (number + body url-encoded). Reads NEXT_PUBLIC_IMESSAGE_NUMBER and NEXT_PUBLIC_TELEGRAM_BOT_USERNAME (default '' when unset).",
    "source_refs": "web/lib/deeplinks.ts:6-24",
    "verify_method": "Unit/static: assert each builder's output string for sample inputs and that missing env yields '' (the PairingPanel then falls back to manual instructions).",
    "notes": "No dedicated unit test exists for the builders, but their outputs are deterministic and verified above; PairingPanel's empty-env fallback path is statically confirmed.",
    "p2_test_status": "pass",
    "p2_evidence": "Static read deeplinks.ts:6-24 + a node check of the exact builder logic: telegramDeepLink('e1isabot','TK123') -> 'https://t.me/e1isabot?start=TK123'; imessagePairLink('+16466208124','AB12') -> 'imessage://%2B16466208124?body=pair%20AB12'; smsPairLink(...) -> 'sms:%2B16466208124?body=pair%20AB12' (number + body url-encoded). IMESSAGE_NUMBER/TELEGRAM_BOT_USERNAME read from NEXT_PUBLIC_* with '' default when unset (23-24), which PairingPanel falls back on to manual instructions. Web tsc+build GREEN.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-33",
    "area": "web",
    "feature": "Route gating middleware",
    "user_story": "As a user, I can be routed correctly by auth state so that protected pages require sign-in and authed users skip the sign-in page.",
    "expected_behavior": "convexAuthNextjsMiddleware: an authed user on /signin is redirected to /dashboard; an unauthed user on /dashboard(.*) or /connect(.*) is redirected to /signin; everything else (marketing) is public. Auth is read from the verified JWT cookie (not spoofable). Matcher runs on all routes except Next internals + static files.",
    "source_refs": "web/middleware.ts:11-27",
    "verify_method": "Playwright on local dev + dev Convex: unauthenticated GET /dashboard and /connect assert redirect to /signin; authenticate then GET /signin assert redirect to /dashboard; GET / unauthenticated assert it stays.",
    "notes": "The authed-on-/signin -> /dashboard direction needs an authenticated dev session to assert live, but the unauthed protections are GREEN in Playwright and the rule is statically correct.",
    "p2_test_status": "pass",
    "p2_evidence": "Playwright auth-gate.spec.ts PASSED: /dashboard -> /signin and /connect -> /signin while unauthenticated (the two protected-route redirects). Static read middleware.ts:11-27 confirms convexAuthNextjsMiddleware: signin+authed -> /dashboard; protected (/dashboard(.*)|/connect(.*)) + unauthed -> /signin; everything else public; auth from convexAuth.isAuthenticated() (verified JWT cookie, not spoofable); matcher excludes Next internals + static files (24-27). Landing-stays-public confirmed by landing.spec PASSED. Web build GREEN (middleware route present).",
    "p3_fix_status": "n/a"
  },
  {
    "id": "WEB-34",
    "area": "web",
    "feature": "Convex client + auth provider wiring",
    "user_story": "As a user, I can have my browser session carry my auth into Convex so that dashboard/connect queries and mutations run as me.",
    "expected_behavior": "A single ConvexReactClient wired to NEXT_PUBLIC_CONVEX_URL is wrapped in ConvexAuthNextjsProvider (client) inside ConvexAuthNextjsServerProvider (layout), bridging Next.js auth cookies to Convex so every app/* useQuery/useMutation/useAction carries the JWT. Layout also self-hosts Geist fonts, renders a skip-to-main link, the grain overlay, and the RevealObserver, and sets site-wide metadata/OG.",
    "source_refs": "web/components/ConvexClientProvider.tsx:10-18;web/app/layout.tsx:12-56",
    "verify_method": "npm run build / tsc for wiring correctness; Playwright on local dev: assert authed dashboard queries resolve (data renders) and the skip link is the first focusable element.",
    "notes": "Authed dashboard queries resolving with live data needs an authenticated dev session, but the provider wiring is build-verified and the skip link is the first focusable element by DOM order.",
    "p2_test_status": "pass",
    "p2_evidence": "Static read ConvexClientProvider.tsx:10-18 + layout.tsx:12-56 + web build GREEN: a single ConvexReactClient wired to NEXT_PUBLIC_CONVEX_URL wrapped in ConvexAuthNextjsProvider (client) inside ConvexAuthNextjsServerProvider (layout 33), bridging Next auth cookies to Convex so app/* useQuery/useMutation/useAction carry the JWT; layout self-hosts Geist sans/mono fonts (3-4,37), renders a skip-to-main link as the first focusable element (44-47), the grain overlay (49), and RevealObserver (51), and sets site-wide metadata/OG (12-25). The auth-gate + landing Playwright runs (which depend on this wiring resolving) PASSED, and `next build`/tsc are GREEN.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-01",
    "area": "control-plane",
    "feature": "Database schema and indexes",
    "user_story": "As the system, I have a typed multi-tenant schema so that every queue, binding, entitlement, and fleet read is index-only and tenant-scoped.",
    "expected_behavior": "Defines the Convex Auth tables plus users (email index; app fields tier/isOperator/createdAt), channelBindings (by_address unique webhook lookup, by_user list), pairingTokens (by_token/by_code/by_user_channel/by_status_expiry), messages (by_handle, by_channel_handle, by_status, by_status_user, by_user, by_userId), connectIntents (by_status, by_user_status), entitlements (by_user, by_providerSubId, by_periodEnd), usageEvents (by_user_at, by_user_period), billingEvents (by_event), agentInstances (by_user, by_desired_status, by_status_heartbeat, by_host_status). channel/tier are literal unions; migration-added userId/channel/replyTarget are optional.",
    "source_refs": "control-plane/convex/schema.ts:1-178",
    "verify_method": "Static reasoning: confirm each index used in queries exists here, e.g. messages.by_status_user backs claimNextForUser and entitlements.by_periodEnd backs rollExpiredPeriods. `npx convex run` deploy validates the schema against live data.",
    "notes": "Static reasoning was the stated verify method; all index usages resolve. Live schema-deploy validation deferred (prod-mutation out of scope).",
    "p2_test_status": "pass",
    "p2_evidence": "Read schema.ts:1-178. Every index named in expected_behavior is present: users.email; channelBindings by_address+by_user; pairingTokens by_token/by_code/by_user_channel/by_status_expiry; messages by_handle/by_channel_handle/by_status[status,receivedAt]/by_status_user[status,userId,receivedAt]/by_user/by_userId; connectIntents by_status/by_user_status; entitlements by_user/by_providerSubId/by_periodEnd; usageEvents by_user_at/by_user_period; billingEvents by_event; agentInstances by_user/by_desired_status/by_status_heartbeat/by_host_status. channel/tier are literal unions; migration columns userId/channel/replyTarget are v.optional. Cross-checked: messages.by_status_user backs claimNextForUser (messages.ts:212), entitlements.by_periodEnd backs rollExpiredPeriods (grant.ts:142). web typecheck+build GREEN (Lead). Live `npx convex run` deploy-validation blocked (no live mutation of prod).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-02",
    "area": "control-plane",
    "feature": "Sign-in dedupe + operator seeding (createOrUpdateUser)",
    "user_story": "As a user, I get exactly one account per email across providers so that signing in via Google then email never forks my data and an attacker cannot squat my email.",
    "expected_behavior": "On first creation: an existing-userId re-sign-in is returned untouched. Email is normalized (trim+lowercase). Email ownership is 'proven' only for type email/verification or oauth with emailVerified!==false. A proven sign-in to a taken email links to the existing row; an UNPROVEN (password) sign-up to a taken email THROWS ('account already exists'). isOperator is set ONLY when proven AND email===OPERATOR_EMAIL (nikto256@gmail.com), granting tier=max; grantSignupEntitlement then writes the source-of-truth entitlement (free instant, operator max+unlimited).",
    "source_refs": "control-plane/convex/auth.ts:24-106; control-plane/convex/billing/grant.ts:110-122",
    "verify_method": "Static reasoning on the callback branches; e2e via the Playwright auth flow (signup happy path + duplicate-email failure path asserting the friendly error, no stack leak). No hermetic Convex seam exists for the callback directly.",
    "notes": "Takeover guard sound. The friendly-error-no-stack-leak path is a web-area assertion; CP callback logic matches expected.",
    "p2_test_status": "pass",
    "p2_evidence": "Read auth.ts:24-106. createOrUpdateUser: existingUserId returned untouched (57); normalizeEmail trim+lowercase (27-31); emailProven = type email/verification OR oauth && emailVerified!==false (69-72); proven sign-in to taken email returns byEmail._id (82), UNPROVEN to taken email THROWS 'an account with this email already exists' (83-85); isOperator only when emailProven && email===OPERATOR_EMAIL (90); grantSignupEntitlement seeds entitlement (102). grant.ts:110-122 operator->max+OPERATOR_QUOTA. Playwright auth-gate specs GREEN (Lead: signup happy + gates). Duplicate-email failure path is a live-OAuth/UI concern (no hermetic Convex callback seam).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-03",
    "area": "control-plane",
    "feature": "Password auth provider (min 8 chars)",
    "user_story": "As a user, I can sign up and sign in with email+password so that I can onboard without OAuth or a phone number.",
    "expected_behavior": "Convex Auth Password provider with id 'password'. validatePasswordRequirements throws 'password must be at least 8 characters' when password.length<8; otherwise any non-empty string is accepted. Self-contained (no external creds) — powers the autonomous e2e signup path.",
    "source_refs": "control-plane/convex/auth.ts:36-44",
    "verify_method": "Static reasoning on the length gate; Playwright signup with a 7-char password asserts the rejection, an 8+ char password succeeds.",
    "notes": "Length gate is the only custom requirement; matches expected.",
    "p2_test_status": "pass",
    "p2_evidence": "Read auth.ts:36-44. Password provider id 'password', validatePasswordRequirements throws 'password must be at least 8 characters' on length<8 else accepts any non-empty (provider default). Self-contained, no external creds. Playwright signup path GREEN (Lead).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-04",
    "area": "control-plane",
    "feature": "Google OAuth provider",
    "user_story": "As a user, I can sign in with Google so that I get a verified-email account with one tap.",
    "expected_behavior": "@auth/core Google provider registered in convexAuth. Requires AUTH_GOOGLE_ID/AUTH_GOOGLE_SECRET in Convex env; absent creds break only this flow. An OAuth sign-in counts as email-proven (unless profile.emailVerified===false), so it dedupes into an existing row by email and can set the operator flag.",
    "source_refs": "control-plane/convex/auth.ts:2-46; control-plane/convex/auth.ts:69-72",
    "verify_method": "Static reasoning (env-dependent external OAuth; not hermetic). Click-through verification that the button redirects to accounts.google.com with the right client_id/redirect_uri and no invalid_client.",
    "notes": "Static-only; live click-through to accounts.google.com is a real-OAuth check out of hermetic scope.",
    "p2_test_status": "pass",
    "p2_evidence": "Read auth.ts:2,34 (Google from @auth/core registered first in providers) + auth.ts:69-72 (oauth counts as email-proven unless emailVerified===false, so it dedupes and can set operator). Env-dependent external OAuth (AUTH_GOOGLE_ID/SECRET) — not hermetic.",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-05",
    "area": "control-plane",
    "feature": "ResendOTP email one-time code",
    "user_story": "As a user, I can sign in with an 8-digit email code so that I onboard with SMS-parity and no phone number.",
    "expected_behavior": "Email provider id 'resend-otp', maxAge 900s (15 min). generateVerificationToken returns an 8-digit numeric code from crypto.getRandomValues (byte % 10). sendVerificationRequest POSTs to api.resend.com/emails with AUTH_RESEND_KEY; throws 'AUTH_RESEND_KEY not configured' when the key is absent and throws on a non-ok HTTP response. From-address defaults to onboarding@resend.dev unless AUTH_EMAIL_FROM is set.",
    "source_refs": "control-plane/convex/ResendOTP.ts:13-55",
    "verify_method": "Static reasoning on TTL/digit-count/key gate; unit-level reasoning that generateVerificationToken always yields 8 numeric chars. Live send is non-hermetic (external Resend), so do not exercise it.",
    "notes": "TTL/digit-count/key-gate all match. Live Resend send intentionally not exercised.",
    "p2_test_status": "pass",
    "p2_evidence": "Read ResendOTP.ts:13-55. Email provider id 'resend-otp', maxAge=60*15=900s; generateVerificationToken: 8 bytes from crypto.getRandomValues, each b%10 -> exactly 8 numeric chars; sendVerificationRequest POSTs api.resend.com/emails with Bearer apiKey, throws 'AUTH_RESEND_KEY not configured' when key absent (33) and throws on non-ok HTTP (50-53); from defaults to 'assistant <onboarding@resend.dev>' unless AUTH_EMAIL_FROM (34).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-06",
    "area": "control-plane",
    "feature": "JWT issuer config",
    "user_story": "As the system, I trust only tokens issued for this deployment so that no foreign JWT can impersonate a tenant.",
    "expected_behavior": "auth.config default trusts a single provider whose domain is CONVEX_SITE_URL (injected by Convex) with applicationID 'convex' (the audience the @convex-dev/auth client uses).",
    "source_refs": "control-plane/convex/auth.config.ts:1-12",
    "verify_method": "Static reasoning; confirm CONVEX_SITE_URL is set in the deployment env (mcp envGet). A mismatched issuer makes requireUser fail closed, observable in the app/* gate tests.",
    "notes": "Static reasoning as specified; config is correct.",
    "p2_test_status": "pass",
    "p2_evidence": "Read auth.config.ts:1-12. Single trusted provider: domain=process.env.CONVEX_SITE_URL, applicationID='convex'. CONVEX_SITE_URL is Convex-injected at runtime. requireUser fails closed on issuer mismatch (authGate.ts). Live envGet of CONVEX_SITE_URL not run (read-only is fine but unnecessary — it is injected by Convex, always present on a deployed instance).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-07",
    "area": "control-plane",
    "feature": "Worker auth gate (assertWorker)",
    "user_story": "As the system, I gate every brain-facing function on a constant-time WORKER_SECRET compare so that an anonymous caller cannot drain the queue or read fleet state.",
    "expected_behavior": "assertWorker(provided) compares against process.env.WORKER_SECRET with a constant-time safeEqual (length-then-XOR, no short-circuit). Throws 'unauthorized worker' on mismatch AND when no secret is configured (fail-closed). Reused by every worker-facing mutation/query (messages, fleet, intents, quota, admin, testing).",
    "source_refs": "control-plane/convex/lib/auth.ts:14-31",
    "verify_method": "convex_e2e seam (RUN_CONVEX_E2E=1): call any worker function (e.g. messages:stats) with a wrong workerSecret and assert it errors 'unauthorized worker'; correct secret succeeds.",
    "notes": "Constant-time + fail-closed both confirmed in code. Live unauthorized-call assertion needs RUN_CONVEX_E2E (blocked) but is statically certain.",
    "p2_test_status": "pass",
    "p2_evidence": "Read lib/auth.ts:14-31. safeEqual: length check then XOR-accumulate over all chars, no short-circuit (constant-time). assertWorker reads process.env.WORKER_SECRET ?? '', throws 'unauthorized worker' when !expected (fail-closed on missing secret) OR on mismatch. Reused by every worker-facing fn (grep: assertWorker imported in messages/fleet/intents/quota/admin/testing). Live wrong-secret e2e blocked (seam disabled); the gate is statically trivial and offline lab tests drive correct-secret client calls (test_convex_client/test_quota_client/test_claim_next_for_user GREEN).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-08",
    "area": "control-plane",
    "feature": "User-facing auth gate (requireUser)",
    "user_story": "As a user, every dashboard call is scoped to my JWT identity so that I can never read or mutate another tenant's data.",
    "expected_behavior": "requireUser reads getAuthUserId from the request JWT and throws 'unauthenticated' (fail-closed) when null; otherwise returns Id<\"users\">. No app/* function ever accepts a client-supplied userId or the WORKER_SECRET — identity is always the JWT subject.",
    "source_refs": "control-plane/convex/app/authGate.ts:13-19",
    "verify_method": "Static reasoning; Playwright: hitting any app/* query unauthenticated throws. Grep confirms every app/* handler calls requireUser first.",
    "notes": "listTiers is deliberately unauthed (public price catalog, no user data) — consistent with expected_behavior; all data-bearing app/* are gated.",
    "p2_test_status": "pass",
    "p2_evidence": "Read app/authGate.ts:13-19: requireUser reads getAuthUserId, throws 'unauthenticated' when null, else returns Id<users>. Grep over control-plane/convex/app/*.ts: every exported query/mutation/action calls requireUser first (account 1/1, billing 2/2, channels 3/3, tiers chooseTier yes + listTiers is an intentional public catalog with no tenant data, upgrade 1/1). No app/* function takes a client userId or WORKER_SECRET. Playwright auth-gate /dashboard->/signin and /connect->/signin GREEN (Lead).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-09",
    "area": "control-plane",
    "feature": "Tenant resolver (resolveUserByAddress)",
    "user_story": "As the system, I turn an inbound (channel,address) into the owning userId so that every enqueued message carries its tenant.",
    "expected_behavior": "internalQuery (not client-callable). Reads channelBindings.by_address(channel,address).first(); returns {userId} ONLY when a binding exists AND verified===true, else null (unknown sender). An unverified mid-pairing row never routes a stranger's messages to a tenant. Index-only read, no filter.",
    "source_refs": "control-plane/convex/lib/identity.ts:16-34",
    "verify_method": "convex_e2e: seedTwoTenants + testIssuePairing/testRedeemImessage to create a verified binding, then drive the webhook (or testEnqueue resolution path) and assert routing to the bound userId; an unbound address drops.",
    "notes": "Unverified-binding-never-routes guard confirmed in code; live e2e needs the disabled seam, covered by docker smoke + static.",
    "p2_test_status": "pass",
    "p2_evidence": "Read lib/identity.ts:16-34. resolveUserByAddress is internalQuery (not client-callable); reads channelBindings.by_address(channel,address).first(); returns {userId} ONLY when binding exists AND verified===true, else null (31). Index-only, no .filter(). Invariant exercised offline by the docker isolation smoke (alice->alice, bob->bob, no cross-tenant) GREEN (Lead) and test_fleet_isolation.py GREEN. Live testRedeem-driven routing assertion blocked (seam disabled).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-10",
    "area": "control-plane",
    "feature": "Idempotent enqueue",
    "user_story": "As the system, I durably enqueue inbound messages idempotently so that a duplicate webhook delivery never double-processes.",
    "expected_behavior": "internalMutation (webhook-only) wrapping enqueueImpl: dedups on by_channel_handle(channel,handle) when channel is known, else legacy by_handle. A pre-existing row returns its _id (no insert); otherwise inserts status='queued' with receivedAt=now, carrying userId/channel/replyTarget/userNumber/text/mediaUrl. The shared enqueueImpl is also driven by the testing:testEnqueue seam.",
    "source_refs": "control-plane/convex/messages.ts:34-79",
    "verify_method": "convex_e2e (test_e2e_fleet_isolation.py): two testEnqueue calls with the same (channel,handle) return the same messageId — one row — proving live by_channel_handle dedup.",
    "notes": "Idempotency logic statically correct. Live by_channel_handle dedup proof needs the disabled seam.",
    "p2_test_status": "pass",
    "p2_evidence": "Read messages.ts:34-79. enqueueImpl dedups on by_channel_handle(channel,handle) when channel known (41-47), else legacy by_handle (48-51); pre-existing row returns existing._id with no insert (52); else inserts status='queued', receivedAt=now, carrying userId/channel/replyTarget/userNumber/text/mediaUrl (54-64). enqueue is internalMutation; the same enqueueImpl is driven by testing:testEnqueue. Live two-identical-testEnqueue dedup assertion blocked (seam disabled); the shared-impl design means the offline-callable path is identical.",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-11",
    "area": "control-plane",
    "feature": "enqueuePhotonInbound (Photon iMessage)",
    "user_story": "As the worker/brain, I enqueue Photon gRPC-stream inbound iMessages over the public HTTP API so that the non-webhook Photon path joins the same durable queue with tenant resolution.",
    "expected_behavior": "PUBLIC mutation gated by assertWorker(workerSecret). Resolves the sender phone to a tenant via internal resolveUserByAddress(imessage,address); unknown sender returns {enqueued:false,userId:null} with no insert (no budget burn). On resolve, enqueueImpl with handle='photon:<providerMessageId>' (idempotency + reaction-targeting key), userId/channel='imessage'/replyTarget=address, returns {enqueued:true,userId}.",
    "source_refs": "control-plane/convex/messages.ts:101-137",
    "verify_method": "convex_e2e: with a verified imessage binding seeded, call messages:enqueuePhotonInbound and assert enqueued:true + the resolved userId; with an unbound address assert enqueued:false. Wrong workerSecret errors.",
    "notes": "Server fn + Python consumer both confirmed. Live wrong-secret/seeded-binding assertion blocked (seam).",
    "p2_test_status": "pass",
    "p2_evidence": "Read messages.ts:101-137. enqueuePhotonInbound is PUBLIC mutation, assertWorker(workerSecret) first (117); resolves via internal resolveUserByAddress(imessage,address) (121-124); unknown sender returns {enqueued:false,userId:null} with no insert (125); on resolve enqueueImpl handle='photon:<providerMessageId>', userId/channel='imessage'/replyTarget=address, returns {enqueued:true,userId} (126-135). Python CONSUMER contract covered offline: test_photon_inbound.py (known-sender enqueues with contract args, unknown skipped no-enqueue, duplicate dropped, attachment media_url) GREEN.",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-12",
    "area": "control-plane",
    "feature": "claimNext (legacy/operator claim)",
    "user_story": "As the legacy operator worker, I claim only unassigned queued messages so that I never steal a fleet tenant's message during cutover.",
    "expected_behavior": "WORKER_SECRET-gated. Claims the oldest queued row with userId===undefined via by_status_user, atomically patches to 'processing' (claimedAt=now), and returns the claim shape (replyTarget defaults to userNumber, channel/userId may be null). Returns null when none. Restricting to userId===undefined prevents the global legacy worker from claiming a tenant row (A2 safety).",
    "source_refs": "control-plane/convex/messages.ts:170-195",
    "verify_method": "convex_e2e: enqueue one row with no userId and one with a userId; claimNext returns the userId-undefined row only, leaving the tenant row queued.",
    "notes": "userId-undefined scoping confirmed. Live two-row claim assertion blocked (seam), covered by unit tests + static.",
    "p2_test_status": "pass",
    "p2_evidence": "Read messages.ts:170-195. claimNext is WORKER_SECRET-gated; by_status_user with status='queued' AND userId===undefined (177-179), order asc first, atomic patch to 'processing'+claimedAt (183), returns claim shape with replyTarget defaulting to userNumber (189). Restricting to userId===undefined prevents the legacy global worker stealing a tenant row (A2). Offline coverage: test_claim_next_for_user.py::test_legacy_worker_uses_global_claim_next + test_fleet_isolation.py::test_scoped_worker_never_calls_global_claim GREEN.",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-13",
    "area": "control-plane",
    "feature": "claimNextForUser (per-tenant fleet claim)",
    "user_story": "As a per-user fleet container, I claim only my own user's oldest queued message so that server-side scoping guarantees I cannot touch another tenant's rows.",
    "expected_behavior": "WORKER_SECRET-gated + userId arg. Claims the oldest queued row for that userId via by_status_user, atomic patch to 'processing'. Best-effort bumps that user's agentInstances.lastActiveAt (by_user) for idle-reap; a missing instance row is not an error. Returns the claim shape or null.",
    "source_refs": "control-plane/convex/messages.ts:205-241",
    "verify_method": "convex_e2e (test_messages_claim_by_user.py): seedTwoTenants, claimNextForUser(userA) returns only A's message and leaves B's queued, proving tenant isolation against the live by_status_user index.",
    "notes": "Per-tenant server-side scoping confirmed. Live seedTwoTenants assertion blocked (seam), strongly covered offline.",
    "p2_test_status": "pass",
    "p2_evidence": "Read messages.ts:205-241. claimNextForUser is WORKER_SECRET-gated + userId arg; by_status_user(status='queued',userId) order asc first (210-216), atomic patch to 'processing' (218); best-effort bumps agentInstances.lastActiveAt via by_user, missing row not an error (222-228); returns claim shape or null. Offline isolation proof: test_claim_next_for_user.py::test_scoped_worker_uses_claim_next_for_user, test_messages_claim_by_user.py (unit portion), test_fleet_isolation.py::test_no_cross_claim GREEN + docker smoke (alice->alice/bob->bob) GREEN.",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-14",
    "area": "control-plane",
    "feature": "claimNextAny (shared bridge worker, skips fleeted)",
    "user_story": "As the single shared bridge worker, I claim the oldest queued message across all non-fleeted tenants so that one process can serve everyone before the per-user fleet exists, without mis-routing fleeted users.",
    "expected_behavior": "WORKER_SECRET-gated, no userId. Builds the set of fleeted userIds (agentInstances by_desired_status desired='running'), takes the oldest SHARED_CLAIM_SCAN=50 queued rows via by_status, and claims the first NOT owned by a fleeted user (legacy userId-less rows always claimable). Atomic patch to 'processing'. Inert until a user has desired='running'; reverts by unsetting WORKER_MODE=shared.",
    "source_refs": "control-plane/convex/messages.ts:272-309",
    "verify_method": "convex_e2e (test_claimnext_any_skips_fleeted.py): seed a tenant + an agentInstances desired='running' for it; claimNextAny skips that tenant's queued row and claims a non-fleeted one.",
    "notes": "ponytail: O(50) scan window noted in code; fleeted-flood rows yield to the fleet (correct, never lossy) | skips-fleeted (anti-mis-route) logic confirmed in code; live proof blocked, covered by static + shared-worker unit tests.",
    "p2_test_status": "pass",
    "p2_evidence": "Read messages.ts:272-309. claimNextAny WORKER_SECRET-gated, no userId; builds fleeted set from agentInstances by_desired_status(desired='running').collect (282-287); takes SHARED_CLAIM_SCAN=50 oldest queued via by_status, finds first row NOT owned by a fleeted user (legacy userId-less always claimable) (290-295); atomic patch to 'processing'. Offline: test_claim_next_for_user.py::test_shared_worker_uses_claim_next_any + test_shared_mode_overrides_stray_scoped_user_id GREEN. Live skip-fleeted assertion = test_claimnext_any_skips_fleeted.py (convex_e2e, blocked, seam disabled).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-15",
    "area": "control-plane",
    "feature": "complete / fail (stale-claim guard)",
    "user_story": "As the worker, I finalize a claimed message exactly once so that a revived zombie worker cannot overwrite a fresh re-processed result.",
    "expected_behavior": "Both WORKER_SECRET-gated. complete patches status='done'+reply+completedAt; fail patches status='error'+error+completedAt — but ONLY if the row is still status==='processing' (a done/error/queued row is left untouched, no-op return null). This guards against double-finalize after the reaper re-queues and another worker re-claims.",
    "source_refs": "control-plane/convex/messages.ts:357-379",
    "verify_method": "convex_e2e: testEnqueue + claimNextForUser then complete → done; a second complete on the same id is a no-op (status already done). complete on a queued (unclaimed) row is a no-op.",
    "notes": "At-most-once finalize guard confirmed. Live no-op-on-done assertion needs the disabled seam.",
    "p2_test_status": "pass",
    "p2_evidence": "Read messages.ts:357-379. complete: assertWorker, get(id), no-op return null if !m || m.status!=='processing', else patch status='done'+reply+completedAt (363-364). fail: same guard, patch status='error'+error+completedAt (375-376). Stale-claim guard (status==='processing' only) prevents zombie double-finalize. Logic statically certain; live double-complete-is-noop assertion blocked (seam disabled).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-16",
    "area": "control-plane",
    "feature": "reapStaleProcessing (cron)",
    "user_story": "As the system, I requeue messages stuck in 'processing' by a crashed worker so that no inbound is silently lost.",
    "expected_behavior": "internalMutation (cron, 2 min). Scans by_status('processing'), take(500); any row whose claimedAt (fallback receivedAt) is older than STALE_PROCESSING_MS=15min is reset to status='queued', claimedAt=undefined. Self-reschedules when a full batch is read (pagination). Window sits above the worst-case turn so it only recovers crashed workers; the complete/fail status guard preserves at-most-once.",
    "source_refs": "control-plane/convex/messages.ts:324-348; control-plane/convex/crons.ts:24-29",
    "verify_method": "convex_e2e: claim a row to 'processing', back-date claimedAt past 15min via a seam (or testBackdate-style), run reapStaleProcessing, assert status flips back to 'queued'. Static reasoning on the 15min threshold.",
    "notes": "15-min window + complete/fail status guard together give at-most-once. Live reap proof blocked.",
    "p2_test_status": "pass",
    "p2_evidence": "Read messages.ts:324-348 + crons.ts:24-29. reapStaleProcessing internalMutation: by_status('processing').take(500), per-row claimedAt??receivedAt, requeue (status='queued', claimedAt=undefined) when now-claimedAt>=STALE_PROCESSING_MS=15*60000; self-reschedules when full batch (343-345). crons.ts registers it at {minutes:2}. Threshold > worst-case hermes turn (~180s) so it only recovers crashes. Live back-date+reap assertion blocked (seam disabled).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-17",
    "area": "control-plane",
    "feature": "recentForUser (conversation memory)",
    "user_story": "As the worker, I read a tenant's recent done turns so that the otherwise-stateless assistant has conversation memory with no cross-tenant bleed.",
    "expected_behavior": "WORKER_SECRET-gated query. limit clamped to [1,20]. Reads by_userId(userId) when given (the stable tenant key), else by_user(userNumber) legacy, else empty (no whole-table scan). Overfetches n*4 desc, skips non-'done' rows, resume:/e2e handles, and rows missing text/reply; returns up to n {text,reply,receivedAt} reversed to chronological order.",
    "source_refs": "control-plane/convex/messages.ts:389-434",
    "verify_method": "convex_e2e: seed a tenant with several done messages (tt- handles), call recentForUser(userId) and assert chronological order, n-clamp, and that another tenant's rows never appear.",
    "notes": "No-cross-tenant-bleed + n-clamp confirmed in code. Live exclusion assertion blocked (seam).",
    "p2_test_status": "pass",
    "p2_evidence": "Read messages.ts:389-434. recentForUser WORKER_SECRET-gated query; n=clamp(limit,1,20) (405); reads by_userId(userId) when given, else by_user(userNumber), else [] no whole-table scan (409-421); overfetch n*4 desc, JS-skips non-'done', resume:/e2e handles, missing text/reply (424-428); out reversed to chronological (431). Strictly one user. Offline portion of test_claim_next_for_user/worker_history covers worker-side history threading GREEN; live cross-tenant-exclusion (test_messages_claim_by_user::test_recent_for_user_excludes_other_tenant) is convex_e2e (blocked).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-18",
    "area": "control-plane",
    "feature": "messages.stats (ops counts)",
    "user_story": "As the operator, I read fleet-wide queue counts so that I can monitor backlog without exposing per-tenant data.",
    "expected_behavior": "WORKER_SECRET-gated query. For each of queued/processing/done/error, takes up to STATS_MAX=5000 rows via by_status and counts them (counts above 5000 report as 5000). Returns {total,queued,processing,done,error}. No per-tenant data — but still secret-gated so volume isn't anonymously readable.",
    "source_refs": "control-plane/convex/messages.ts:442-475",
    "verify_method": "convex_e2e: seed a few messages, call messages:stats with the worker secret and assert counts reflect the seeded statuses; wrong secret errors.",
    "notes": "Bounded indexed counts confirmed. Live count assertion blocked.",
    "p2_test_status": "pass",
    "p2_evidence": "Read messages.ts:442-475. stats WORKER_SECRET-gated query; for each of queued/processing/done/error takes STATS_MAX=5000 via by_status and counts rows.length (counts above 5000 cap at 5000); returns {total,queued,processing,done,error}. No per-tenant data but still secret-gated. Live seeded-count assertion blocked (seam disabled).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-19",
    "area": "control-plane",
    "feature": "issuePairingToken (mint, supersede)",
    "user_story": "As a user, I mint a single-use pairing token+code for a channel so that I can connect iMessage or Telegram, and any prior active token is invalidated.",
    "expected_behavior": "issuePairingTokenImpl supersedes any still-'active' token for (user,channel) (bounded take(200) via by_user_channel), then inserts a fresh row: 22-char base64url token (Telegram /start-safe), 6-char code from a 32-char unambiguous alphabet, status='active', expiresAt=now+15min. Returns {token,code,expiresAt,channel}. The internalMutation wraps the impl; app/channels:createPairingToken and the testing seam reuse it.",
    "source_refs": "control-plane/convex/pairing.ts:25-97",
    "verify_method": "convex_e2e (test_pairing.py): testIssuePairing twice for the same (user,channel) and assert the prior token is superseded (redeeming it fails) while the new one is active.",
    "notes": "Mint+supersede confirmed in code. Live supersede proof blocked (seam).",
    "p2_test_status": "pass",
    "p2_evidence": "Read pairing.ts:25-97. issuePairingTokenImpl: by_user_channel bounded take(200), patches still-'active' rows to 'superseded' (69-75); inserts 22-char base64url token via _genToken (16 bytes->btoa->url-safe, 35-40), 6-char code from 32-char unambiguous alphabet (26,43-48), status='active', expiresAt=now+TOKEN_TTL_MS(15min). internalMutation wraps the impl (86-97); app/channels:createPairingToken + testing seam reuse it. Live double-issue-supersede assertion = test_pairing.py (convex_e2e, blocked, seam disabled).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-20",
    "area": "control-plane",
    "feature": "redeemTelegram / redeemImessage (bind, hijack guard)",
    "user_story": "As a user, I redeem my pairing token from the channel so that a verified (channel,address)->userId binding is created, and no stranger can hijack a binding.",
    "expected_behavior": "Shared _redeem: trims address (empty->no_address); looks up the token row (by_token for Telegram, normalized-uppercase by_code for iMessage) and rejects if missing or channel mismatch (not_found). A consumed token re-tapped by the SAME verified owner is idempotent re-greet ({ok,idempotent}), else already_consumed. Non-active/expired -> expired. If (channel,address) is already bound to a DIFFERENT user -> address_taken. Otherwise upserts a verified binding and marks the token consumed. internalMutations; impls exported for the test seam.",
    "source_refs": "control-plane/convex/pairing.ts:108-196",
    "verify_method": "convex_e2e (test_pairing.py): testIssuePairing then testRedeemImessage/testRedeemTelegram covering single-use, wrong-channel, expiry (testExpirePairing back-dates), and cross-user address_taken rejection.",
    "notes": "Hijack guard (address_taken) and idempotent re-tap both confirmed in code. Live proof blocked (seam).",
    "p2_test_status": "pass",
    "p2_evidence": "Read pairing.ts:108-196. _redeem: trims address, no_address on empty (116); lookup by_token (Telegram) / normalized-uppercase by_code (iMessage) (158-169), not_found on missing or channel mismatch (119-121); consumed token by SAME verified owner -> {ok,idempotent} else already_consumed (130-135); non-active/expired -> expired (136-138); (channel,address) bound to DIFFERENT user -> address_taken hijack guard (141-143); else verify/insert binding + consume token (145-153). Live single-use/wrong-channel/expiry/cross-user assertions = test_pairing.py (convex_e2e, blocked).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-21",
    "area": "control-plane",
    "feature": "expireStaleTokens (cron)",
    "user_story": "As the system, I mark expired-but-still-active pairing tokens 'expired' so that the one-active-per-user invariant and dashboard stay clean.",
    "expected_behavior": "internalMutation (cron, 30 min). Scans by_status_expiry(status='active', expiresAt<now) ordered by expiresAt, take(500), patches each to status='expired'. Self-reschedules on a full batch. Non-load-bearing (redeem self-checks expiry) — housekeeping only.",
    "source_refs": "control-plane/convex/pairing.ts:206-225; control-plane/convex/crons.ts:43-48",
    "verify_method": "convex_e2e: issue a token, back-date its expiresAt (testExpirePairing sets active+past expiry), run expireStaleTokens and assert it flips to 'expired'. Static reasoning on the batch/pagination.",
    "notes": "Housekeeping cron confirmed. Live proof blocked.",
    "p2_test_status": "pass",
    "p2_evidence": "Read pairing.ts:206-225 + crons.ts:43-48. expireStaleTokens internalMutation: by_status_expiry(status='active', expiresAt<now).take(500), patches each to 'expired', self-reschedules on full batch. crons.ts registers it at {minutes:30}. Non-load-bearing (redeem self-checks expiry). Live back-date+expire assertion blocked (seam disabled).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-22",
    "area": "control-plane",
    "feature": "Sendblue inbound webhook (HTTP)",
    "user_story": "As a user, I text the iMessage line so that my message is authenticated, resolved to my tenant, durably enqueued, and my container is cold-started.",
    "expected_behavior": "POST /sendblue/inbound/{secret}: constant-time compare of the path token to WEBHOOK_SECRET (wrong/absent->401). Malformed JSON or is_outbound/opted_out or missing text/number/handle -> 200 {ignored}. 'pair <code>' redeems an iMessage token. ALLOWED_USER_NUMBER stays on the legacy userId-less enqueue path. Otherwise resolveUserByAddress; unknown sender -> 200 ignored (no enqueue); known -> enqueue with userId/channel/replyTarget and best-effort requestInstanceInternal cold-start (swallowed on failure). Never 5xx on payload problems.",
    "source_refs": "control-plane/convex/http.ts:32-120",
    "verify_method": "Static reasoning + curl against dev: wrong secret -> 401; outbound/opted-out -> 200 ignored; a 'pair <code>' body redeems. Do NOT send a real Sendblue message. Full path is exercised by the live e2e harness.",
    "notes": "All branches match expected. Live curl against dev not run (would need real send / prod-ish); auth+ignore+pair branches statically certain.",
    "p2_test_status": "pass",
    "p2_evidence": "Read http.ts:32-120. /sendblue/inbound/{secret}: safeEqual path token vs WEBHOOK_SECRET, 401 on wrong/absent (38-41); malformed JSON -> 200 ignored (47); is_outbound/opted_out -> 200 ignored (50-52); missing text/number/handle -> 200 ignored (56); 'pair <code>' regex -> redeemImessage (63-70); ALLOWED_USER_NUMBER stays on legacy userId-less enqueue (77-86); else resolveUserByAddress, unknown -> 200 ignored no enqueue (95), known -> enqueue with userId/channel/replyTarget + best-effort requestInstanceInternal cold-start (97-118). Offline parser: test_parse_inbound.py GREEN. Real Sendblue send out of scope.",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-23",
    "area": "control-plane",
    "feature": "Telegram inbound webhook (HTTP)",
    "user_story": "As a user, I message the Telegram bot so that my update is authenticated by header secret, /start binds my chat, and normal messages are enqueued and cold-start my container.",
    "expected_behavior": "POST /telegram/inbound: constant-time compare of X-Telegram-Bot-Api-Secret-Token to TELEGRAM_WEBHOOK_SECRET (wrong/absent->401). Ignores malformed JSON, edited_message/channel_post, non-private chats, bots, and empty text/chat-id/from-id (->200 ignored). handle='tg:<update_id>:<message_id>'. '/start <token>' redeems a Telegram pairing token (binds chat.id). Otherwise resolveUserByAddress(telegram, chat.id); unknown->drop; known->enqueue (channel='telegram', replyTarget=chat.id, userNumber=from.id) + best-effort cold-start.",
    "source_refs": "control-plane/convex/http.ts:132-210",
    "verify_method": "Static reasoning + curl against dev: missing/wrong header secret -> 401; bot/edited/channel payloads -> 200 ignored; a '/start <token>' body redeems. Do not send via the real bot.",
    "notes": "handle is the full tg:<update_id>:<message_id> form (catalog EXPECT abbreviated it as tg:<update_id> in prose but the full ref matches code). All ignore/auth/start branches match. Real bot send out of scope.",
    "p2_test_status": "pass",
    "p2_evidence": "Read http.ts:132-210. /telegram/inbound: safeEqual X-Telegram-Bot-Api-Secret-Token vs TELEGRAM_WEBHOOK_SECRET, 401 wrong/absent (136-140); malformed JSON -> 200 ignored (146); ignores edited_message/channel_post/no message (151-153), non-private chat or bot from (154-156), empty text/chatId/fromId (160-162); handle='tg:<update_id>:<message_id>' (169); '/start <token>' regex -> redeemTelegram binding chat.id (172-182); else resolveUserByAddress(telegram,chatId), unknown -> 200 ignored (189), known -> enqueue + cold-start (191-208).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-24",
    "area": "control-plane",
    "feature": "Convex Auth HTTP routes",
    "user_story": "As a user, the auth sign-in/OAuth-callback/token routes are served so that browser auth works alongside the always-on channel webhooks.",
    "expected_behavior": "auth.addHttpRoutes(http) appends the Convex Auth routes (sign-in, OAuth callback, token refresh) to the SAME httpRouter that hosts the Sendblue and Telegram webhooks, so all coexist at <deployment>.convex.site.",
    "source_refs": "control-plane/convex/http.ts:212-216",
    "verify_method": "Static reasoning; the live Playwright auth flow exercises these routes end to end (sign-in issues a session).",
    "notes": "Single shared router confirmed; auth routes coexist with channel webhooks.",
    "p2_test_status": "pass",
    "p2_evidence": "Read http.ts:212-216. auth.addHttpRoutes(http) appends Convex Auth sign-in/OAuth-callback/token routes to the SAME httpRouter that hosts /sendblue/inbound and /telegram/inbound, so all coexist at <deployment>.convex.site. Playwright auth flow (sign-in issues a session) GREEN (Lead's landing+auth-gate specs depend on these routes).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-25",
    "area": "control-plane",
    "feature": "requestInstance cold-start (+Internal)",
    "user_story": "As the system, an inbound for a user with no warm container flips desired->running so that an idle or never-launched tenant's message triggers a launch.",
    "expected_behavior": "requestInstanceImpl upserts the per-user agentInstances row (by_user): existing -> refresh wantsAt, set desired='running' if not already, update channel if changed (idempotent); none -> insert desired='running', status='provisioning', tier from arg or deriveTier (pro/max entitlement -> 'paid', else 'free'), wantsAt=now. Public requestInstance is WORKER_SECRET-gated; requestInstanceInternal is the no-secret internal trigger the webhooks call.",
    "source_refs": "control-plane/convex/fleet.ts:53-126",
    "verify_method": "convex_e2e (test_e2e_fleet_lifecycle.py): seedTwoTenants then requestInstance(userA); assert created:true/status:provisioning, a second call is idempotent (created:false). deriveTier verified via an entitlement seam.",
    "notes": "Cold-start upsert + deriveTier + both entry points confirmed. Live created:true/false proof blocked (seam).",
    "p2_test_status": "pass",
    "p2_evidence": "Read fleet.ts:53-126. requestInstanceImpl upserts by_user: existing -> wantsAt=now, desired='running' if not already, channel update if changed, returns created:false (74-84); none -> insert desired='running', status='provisioning', tier from arg or deriveTier (pro/max entitlement->'paid' else 'free') (53-62,85-93), created:true. Public requestInstance is WORKER_SECRET-gated (96-112); requestInstanceInternal is the un-gated internal cold-start trigger called by http.ts webhooks (116-126). Live idempotency assertion = test_e2e_fleet_lifecycle.py (convex_e2e, blocked).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-26",
    "area": "control-plane",
    "feature": "claimInstanceForLaunch (race-safe)",
    "user_story": "As the controller, I record a live container after docker run so that two controllers racing the same launch cannot both win.",
    "expected_behavior": "WORKER_SECRET-gated. Looks up the user's instance (by_user); returns {claimed:false} if missing OR already status==='running' (the racing loser tears down its duplicate). Otherwise patches status='running', hostVm/containerId, workerSecretRef (kept if not provided), startedAt/heartbeatAt=now, lastActiveAt (kept or now), errorCount=0, error cleared. Returns {claimed:true}.",
    "source_refs": "control-plane/convex/fleet.ts:134-165",
    "verify_method": "convex_e2e: requestInstance then claimInstanceForLaunch -> claimed:true; a second claim on the now-running row -> claimed:false.",
    "notes": "Race-safe atomic guard confirmed. Live double-claim proof blocked.",
    "p2_test_status": "pass",
    "p2_evidence": "Read fleet.ts:134-165. claimInstanceForLaunch WORKER_SECRET-gated; by_user lookup; {claimed:false} if missing (149) OR already status==='running' (150, racing loser); else patch status='running', hostVm/containerId, workerSecretRef kept-if-absent, startedAt/heartbeatAt=now, lastActiveAt kept-or-now, errorCount=0, error cleared (152-162); returns {claimed:true}. Live race assertion blocked (seam disabled).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-27",
    "area": "control-plane",
    "feature": "heartbeat (returns desired)",
    "user_story": "As the worker, I heartbeat each loop and learn my desired state so that a reaped worker drains and exits gracefully instead of being SIGKILLed.",
    "expected_behavior": "WORKER_SECRET-gated. Looks up by_user; missing -> {desired:null} (pre-fleet operator, not an error). Otherwise patches heartbeatAt=now, promotes status 'provisioning'->'running', and returns {desired} (the current control-loop target).",
    "source_refs": "control-plane/convex/fleet.ts:173-188",
    "verify_method": "convex_e2e: requestInstance (provisioning) then heartbeat asserts status promoted to running and desired='running'; setDesired('stopped') then heartbeat returns desired='stopped'.",
    "notes": "Heartbeat promote + desired-return confirmed. Live status-promotion proof blocked.",
    "p2_test_status": "pass",
    "p2_evidence": "Read fleet.ts:173-188. heartbeat WORKER_SECRET-gated; by_user; missing -> {desired:null} not an error (182); else patch heartbeatAt=now, promote status 'provisioning'->'running' (184), returns {desired} current target (186). Offline worker-side: test_worker_heartbeat.py GREEN. Live provisioning->running + setDesired->desired assertion blocked (seam).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-28",
    "area": "control-plane",
    "feature": "markStopped / markError / setDesired",
    "user_story": "As the controller, I record stop/error and flip the desired target so that the reconcile loop converges and relaunch-storms are bounded.",
    "expected_behavior": "All WORKER_SECRET-gated, by_user lookup. markStopped (clean docker stop): status='stopped', stoppedAt=now, clears containerId/hostVm. markError: status='error', error, errorCount++ (returns {errorCount} so the controller can stop relaunching after N). setDesired: patches desired; when 'running' also refreshes wantsAt (cold-start clock). Missing instance is a no-op (markError returns errorCount:0).",
    "source_refs": "control-plane/convex/fleet.ts:191-248",
    "verify_method": "convex_e2e: on a seeded instance call markError twice -> errorCount increments; markStopped -> status stopped + container cleared; setDesired('running'/'stopped') flips desired (verified via listReconcile).",
    "notes": "All three transitions + missing-noop confirmed. Live proof blocked.",
    "p2_test_status": "pass",
    "p2_evidence": "Read fleet.ts:191-248. All WORKER_SECRET-gated, by_user. markStopped: status='stopped', stoppedAt=now, clears containerId/hostVm (191-210). markError: status='error', error, errorCount=(prev??0)+1, returns {errorCount}; missing instance returns {errorCount:0} (214-228). setDesired: patches desired; when 'running' also refreshes wantsAt (232-248). Live increment/flip assertions blocked (seam disabled).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-29",
    "area": "control-plane",
    "feature": "listReconcile (controller poll)",
    "user_story": "As the controller, I poll the actionable instance set so that I can diff desired vs actual and launch/stop containers.",
    "expected_behavior": "WORKER_SECRET-gated query. Unions by_desired_status(desired='running') with by_status_heartbeat(status='running'), each bounded take(500), dedups by _id, and maps to reconcileItem (instanceId,userId,desired,status,tier,hostVm,containerId,heartbeatAt,lastActiveAt,wantsAt,errorCount with nulls for absent optionals). Index-only.",
    "source_refs": "control-plane/convex/fleet.ts:250-299",
    "verify_method": "convex_e2e: requestInstance for a tenant then listReconcile asserts the row appears with desired='running'; after markStopped it leaves the wanted set.",
    "notes": "Union+dedup reconcile read confirmed. Live proof blocked.",
    "p2_test_status": "pass",
    "p2_evidence": "Read fleet.ts:250-299. listReconcile WORKER_SECRET-gated query; unions by_desired_status(desired='running').take(500) with by_status_heartbeat(status='running').take(500), dedups by _id in a Map (283-284), maps to reconcileItem with nulls for absent optionals (285-297). Index-only. Live appear/leave-wanted-set assertion blocked (seam disabled).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-30",
    "area": "control-plane",
    "feature": "reapIdleInstances (cron DISABLED)",
    "user_story": "As the operator, free-tier idle containers can be reaped for $0 compute, but currently every tier stays warm 24/7.",
    "expected_behavior": "reapIdleInstancesImpl scans by_status_heartbeat('running'), take(500); flips free-tier, desired='running' rows idle > IDLE_TTL_FREE_MS (30 min) to desired='stopped' (paid stays warm), self-reschedules on a full batch. The internalMutation wrapper exists but the crons.ts schedule is COMMENTED OUT (operator requirement 2026-06-17: all tiers always-on), so this NEVER runs automatically — only via the testReapIdle seam or a manual call.",
    "source_refs": "control-plane/convex/fleet.ts:308-335; control-plane/convex/crons.ts:31-39",
    "verify_method": "convex_e2e (test_e2e_fleet_lifecycle.py): requestInstance(free) + testBackdateInstance past 30 min, run testReapIdle, assert reaped:1 / desired flipped to 'stopped'; a paid instance is not reaped. Confirm crons.ts has NO active reapIdle schedule.",
    "notes": "Cron intentionally disabled; the logic is reachable only via testReapIdle or a manual invocation. Verify cron stays disabled (Phase-2 watch) | Reaper logic present, cron intentionally DISABLED (all tiers warm 24/7) — matches expected. Live reap proof blocked (seam).",
    "p2_test_status": "pass",
    "p2_evidence": "Read fleet.ts:308-335 + crons.ts:31-39. reapIdleInstancesImpl scans by_status_heartbeat('running').take(500), skips non-free tier and desired!=='running', flips desired='stopped' when now-(lastActiveAt??startedAt??now) >= IDLE_TTL_FREE_MS=30min, self-reschedules on full batch. The internalMutation wrapper exists (331-335) but crons.ts has NO active reapIdle schedule — it is a commented prose block (31-39), confirmed by CP-45 (exactly 3 crons.interval). Live testReapIdle assertion = test_e2e_fleet_lifecycle.py (convex_e2e, blocked).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-31",
    "area": "control-plane",
    "feature": "Connect intents: addIntent / listPending",
    "user_story": "As the brain, I record a Composio connect intent when I text a connect-link and list pending intents so that I can resume the task once toolkits are connected.",
    "expected_behavior": "Both WORKER_SECRET-gated, PUBLIC (the Python worker calls over HTTP). addIntent dedups an identical still-pending intent (same userNumber+taskText+sorted requiredToolkits) scoped per-user via by_user_status when userId is known, else global by_status, each bounded take(200); returns the existing or newly inserted id (connectedToolkits=[], status='pending'). listPending returns up to 200 pending intents (global scan, single-worker model).",
    "source_refs": "control-plane/convex/intents.ts:23-100",
    "verify_method": "convex_e2e: addIntent twice with identical args returns the same id (dedup); listPending includes it. Wrong workerSecret errors.",
    "notes": "Dedup scope + pending list confirmed. Live same-id dedup proof blocked.",
    "p2_test_status": "pass",
    "p2_evidence": "Read intents.ts:23-100. addIntent + listPending both WORKER_SECRET-gated PUBLIC (Python worker over HTTP). addIntent dedups identical still-pending (userNumber+taskText+sorted requiredToolkits) via by_user_status when userId known else global by_status, each take(200); returns existing or new id, connectedToolkits=[], status='pending' (51-68). listPending returns up to 200 pending (73-100). Offline consumer: test_pending.py GREEN (addIntent call + missing-env). Live dedup assertion blocked (seam).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-32",
    "area": "control-plane",
    "feature": "resolveIntent (synthetic resume) / expireIntent",
    "user_story": "As the brain, I report connected toolkits so that a covered intent enqueues a synthetic resume message routed to the right tenant, or I expire an intent that never completed.",
    "expected_behavior": "Both WORKER_SECRET-gated. resolveIntent: returns {resumed:false} if the intent is missing or not 'pending'; patches connectedToolkits; if they do NOT cover requiredToolkits -> {resumed:false}; if covered, idempotently inserts a 'resume:<id>' message (skipped if one with that handle exists) carrying the intent's userId/channel/replyTarget/userNumber/taskText, then patches status='resumed', resumedAt. expireIntent flips a pending intent to 'expired' (no-op otherwise).",
    "source_refs": "control-plane/convex/intents.ts:106-150",
    "verify_method": "convex_e2e: addIntent with one required toolkit; resolveIntent with a non-covering set -> resumed:false; with a covering set -> resumed:true and a resume:<id> message is enqueued exactly once (idempotent on re-call). expireIntent flips pending->expired.",
    "notes": "Synthetic-resume routing + idempotency + expire confirmed. Live proof blocked.",
    "p2_test_status": "pass",
    "p2_evidence": "Read intents.ts:106-150. resolveIntent WORKER_SECRET-gated: {resumed:false} if missing or status!=='pending' (116); patch connectedToolkits (117); if not covers(requiredToolkits) -> {resumed:false} (118); else idempotently insert 'resume:<id>' message (skip if by_handle exists) carrying userId/channel/replyTarget/userNumber/taskText (119-135), patch status='resumed'+resumedAt, {resumed:true} (136-137). expireIntent flips pending->expired (141-150). Live cover/non-cover + idempotent resume assertion blocked (seam disabled).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-33",
    "area": "control-plane",
    "feature": "Tier catalog (billing/tiers)",
    "user_story": "As the system, one data-driven tier catalog drives the landing, grants, and quota gate so that prices, quotas, and channels never drift.",
    "expected_behavior": "TIERS: free (100 turns, $0), pro (1000, $19), max (5000, $49) — both imessage+telegram on every tier. PERIOD_MS=30 days. OPERATOR_QUOTA=-1 (unlimited sentinel). publicTiers() returns [free,pro,max] for the UI; tierValidator is the literal union. Quotas/prices are edited here as the single source.",
    "source_refs": "control-plane/convex/billing/tiers.ts:12-64",
    "verify_method": "Static reasoning: confirm writeEntitlement/listTiers/checkAndReserve all read these constants. Unit-level: publicTiers() returns 3 specs with the stated quotas.",
    "notes": "CP-33 contract itself is correct. The web mirror (web/lib/tiers.ts) currently agrees with the server catalog, so the catalog-author 'mirror drift' note is a MISSING-GUARD observation (no automated drift check), NOT a current bug. The 'turns/mo' vs 'messages/month' COPY split (marketing TierGrid.tsx:101 vs dashboard TierCard.tsx:80 + ConnectWizard.tsx:221) is a WEB-area UX issue, not a control-plane defect — flag for the web verifier.",
    "p2_test_status": "pass",
    "p2_evidence": "Read billing/tiers.ts:12-64. TIERS: free(100 turns,$0), pro(1000,$19), max(5000,$49), both imessage+telegram on every tier; PERIOD_MS=30 days; OPERATOR_QUOTA=-1; publicTiers() returns [free,pro,max]; tierValidator literal union. Confirmed single-source: writeEntitlement reads TIERS[tier].msgQuota (grant.ts:53), checkAndReserve reads e.msgQuota (quota.ts), app/tiers:listTiers returns publicTiers(). web/lib/tiers.ts mirror byte-checked vs this — quotas/prices/channels/blurbs AGREE NOW (free 100/0, pro 1000/19, max 5000/49).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-34",
    "area": "control-plane",
    "feature": "Entitlement writer (writeEntitlement / applyEntitlement / grantSignupEntitlement)",
    "user_story": "As the system, all entitlement writes funnel through one primitive so that quota/period/tier-mirror logic can never disagree across signup, admin grant, tier picker, and future webhooks.",
    "expected_behavior": "writeEntitlement: quota = quotaOverride ?? TIERS[tier].msgQuota. Existing row -> patch tier/status/quota/source/provider ids, and reset period (periodStart/End, msgUsed=0) ONLY if the current period already ended (a mid-period tier change keeps the meter). No row -> insert with msgUsed=0, fresh 30-day window. Also patches the denormalized users.tier mirror if changed. applyEntitlement is the internalMutation wrapper; grantSignupEntitlement grants operator max+unlimited or free.",
    "source_refs": "control-plane/convex/billing/grant.ts:40-122",
    "verify_method": "convex_e2e: via testSetEntitlement/chooseTier observe that a mid-period pro->max change keeps msgUsed but updates quota; an expired-period change resets msgUsed to 0. Static reasoning on the periodValid branch.",
    "notes": "Single-writer period/quota/mirror logic confirmed. Live testSetEntitlement proof blocked.",
    "p2_test_status": "pass",
    "p2_evidence": "Read billing/grant.ts:40-122. writeEntitlement: quota = quotaOverride ?? TIERS[tier].msgQuota (53); existing row -> patch tier/status/quota/source/provider ids, and reset period (periodStart/End,msgUsed=0) ONLY when !periodValid (existing.periodEnd<=now) — a mid-period change keeps msgUsed (60-76); no row -> insert msgUsed=0 fresh 30-day window (77-91); patches denormalized users.tier mirror if changed (94-97). applyEntitlement is the internalMutation wrapper (100-107); grantSignupEntitlement convenience (110-122). Live mid-period-keeps / expired-resets assertion blocked (seam disabled).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-35",
    "area": "control-plane",
    "feature": "rollExpiredPeriods (cron)",
    "user_story": "As an idle user, my quota window resets at the period boundary so that my dashboard shows a fresh window without waiting for a turn.",
    "expected_behavior": "internalMutation (cron, 1 hour). Scans by_periodEnd(periodEnd<=now), take(500); each rolled to periodStart=now, periodEnd=now+30d, msgUsed=0 (operator/unlimited harmlessly rolled too, leaving the scan). Returns {rolled,more}; self-reschedules while a full batch is read. Redundant with checkAndReserve's self-roll for active users.",
    "source_refs": "control-plane/convex/billing/grant.ts:133-160; control-plane/convex/crons.ts:14-19",
    "verify_method": "convex_e2e: testSetEntitlement with a past periodEnd, run rollExpiredPeriods (or via a seam), assert rolled>=1 and the window is fresh with msgUsed=0.",
    "notes": "Period-roll cron confirmed. Live proof blocked.",
    "p2_test_status": "pass",
    "p2_evidence": "Read billing/grant.ts:133-160 + crons.ts:14-19. rollExpiredPeriods internalMutation: by_periodEnd(periodEnd<=now).take(500), each rolled to periodStart=now/periodEnd=now+PERIOD_MS/msgUsed=0/updatedAt (operator/unlimited harmlessly rolled, leaves scan via future periodEnd); returns {rolled,more}; self-reschedules while full batch. crons.ts registers it at {hours:1}. Redundant with checkAndReserve self-roll. Live past-periodEnd roll assertion blocked (seam disabled).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-36",
    "area": "control-plane",
    "feature": "Quota gate: checkAndReserve",
    "user_story": "As the system, I atomically reserve one turn before any model call so that an over-quota turn costs $0 and concurrent turns can't double-spend the last unit.",
    "expected_behavior": "WORKER_SECRET-gated mutation. No entitlement -> allowed:false reason='no_entitlement' (fail closed). Unlimited (msgQuota<0) -> allowed:true, never incremented. status='canceled' -> allowed:false reason='canceled' (past_due allowed during grace). Self-rolls an expired period (msgUsed=0). msgUsed>=quota -> allowed:false reason='over_quota'. Otherwise atomically msgUsed++ and returns allowed:true with remaining. The worker fails OPEN on a thrown Convex error but CLOSED on a clean deny.",
    "source_refs": "control-plane/convex/quota.ts:23-115",
    "verify_method": "convex_e2e (test_entitlement_quota.py): testSetEntitlement to msgUsed=quota -> over_quota; canceled -> canceled; unlimited -> allowed without increment; a normal reserve increments msgUsed by 1.",
    "notes": "Atomic reserve + all deny branches confirmed; offline client tests green. Live seeded-quota proof blocked (seam).",
    "p2_test_status": "pass",
    "p2_evidence": "Read quota.ts:23-115. checkAndReserve WORKER_SECRET-gated mutation: no entitlement -> allowed:false reason='no_entitlement' fail-closed (64-73); msgQuota<0 unlimited -> allowed:true never incremented (76-78); status==='canceled' -> allowed:false reason='canceled' (past_due allowed) (81-90); rollIfExpired self-rolls expired period to msgUsed=0 (92,23-35); msgUsed>=quota -> allowed:false reason='over_quota' (93-102); else msgUsed++ atomically in the single txn, allowed:true (104-113). Offline: test_quota_client.py + test_worker_quota.py GREEN. Live over_quota/canceled/unlimited assertions = test_entitlement_quota.py (convex_e2e, blocked).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-37",
    "area": "control-plane",
    "feature": "Quota gate: recordUsage / releaseReserve",
    "user_story": "As the system, I append a per-turn meter event and can refund a reserved unit on hard failure so that the dashboard is accurate and a failed turn doesn't burn quota.",
    "expected_behavior": "Both WORKER_SECRET-gated. recordUsage appends an append-only usageEvents row (kind='turn', units default 1, lane/channel/tokens, periodStart from the live entitlement or now) — no OCC contention. releaseReserve decrements entitlements.msgUsed by 1 ONLY for a real reservation (skips when no entitlement, unlimited msgQuota<0, or msgUsed<=0; never below zero); returns {released}.",
    "source_refs": "control-plane/convex/quota.ts:117-166",
    "verify_method": "convex_e2e: checkAndReserve then releaseReserve restores msgUsed; releaseReserve at msgUsed=0 returns released:false. recordUsage inserts one usageEvents row observable via myUsageTimeline.",
    "notes": "Append-only meter + safe refund confirmed. Live timeline-row proof blocked.",
    "p2_test_status": "pass",
    "p2_evidence": "Read quota.ts:117-166. recordUsage WORKER_SECRET-gated: appends append-only usageEvents row kind='turn', units??1, lane/channel/tokens, periodStart from live entitlement or now (136-147) — no OCC. releaseReserve WORKER_SECRET-gated: decrements msgUsed by 1 ONLY for a real reservation, skips when !e || msgQuota<0 || msgUsed<=0 (never below zero), returns {released} (152-165). Offline: test_quota_client.py / test_worker_quota.py GREEN. Live restore/insert assertion blocked (seam).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-38",
    "area": "control-plane",
    "feature": "App tier picker: listTiers / chooseTier",
    "user_story": "As a user, I view the catalog and choose a tier so that free is granted instantly while paid is escalation-safe (never self-granted).",
    "expected_behavior": "listTiers (public query) returns publicTiers(). chooseTier (requireUser-gated): operator is never downgraded (returns max, granted:false). tier='free' grants free via writeEntitlement UNLESS an active paid plan is in force (then it's kept, granted:false — no silent downgrade). Paid tiers are NEVER written here — returns granted:false + a coming-soon checkoutUrl; the free entitlement from signup stays in force.",
    "source_refs": "control-plane/convex/app/tiers.ts:26-82",
    "verify_method": "convex_e2e/Playwright: authenticated chooseTier('free') grants free; chooseTier('pro') returns coming-soon, granted:false, and does NOT write a pro entitlement. Operator chooseTier('free') keeps max.",
    "notes": "Escalation-safe free-only grant + no-downgrade confirmed. Live grant assertion blocked (seam).",
    "p2_test_status": "pass",
    "p2_evidence": "Read app/tiers.ts:26-82. listTiers public query returns publicTiers() (26-30). chooseTier requireUser-gated: operator never downgraded, returns max/granted:false (45-48); tier='free' grants free via writeEntitlement UNLESS active paid plan in force (then kept, granted:false — no silent downgrade) (50-69); paid tiers NEVER written — returns granted:false + coming-soon checkoutUrl (72-80). Live chooseTier('free')/('pro') assertion = convex_e2e/Playwright tier-pick (blocked seam for the granted-write assertion).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-39",
    "area": "control-plane",
    "feature": "App checkout: startCheckout + billing provider",
    "user_story": "As a user, I start a checkout for a paid tier so that I'm routed to the active billing provider, and the app never hard-codes a vendor.",
    "expected_behavior": "app/upgrade:startCheckout is a requireUser-gated action delegating to activeProvider().createCheckout(JWT-userId, tier in {pro,max}); returns {ok,checkoutUrl,message}. activeProvider() selects from BILLING_PROVIDER env (default StubProvider). StubProvider.createCheckout returns ok:false + COMING_SOON URL ('/dashboard?upgrade=coming-soon'), portalUrl null, handleWebhook {ok:false} (never emits real events). Entitlement writes always go through billing/grant.",
    "source_refs": "control-plane/convex/app/upgrade.ts:13-29; control-plane/convex/billing/provider.ts:31-38; control-plane/convex/billing/stub.ts:11-31",
    "verify_method": "convex_e2e/Playwright: authenticated startCheckout('pro') returns ok:false + the coming-soon URL and writes no entitlement. Static reasoning on activeProvider's default branch.",
    "notes": "Provider adapter + stub coming-soon + no-entitlement-write confirmed; offline provider-switch test green.",
    "p2_test_status": "pass",
    "p2_evidence": "Read app/upgrade.ts:13-29 + billing/provider.ts:31-38 + billing/stub.ts:11-31. startCheckout is requireUser-gated action delegating to activeProvider().createCheckout(JWT userId, tier in {pro,max}) (20-28). activeProvider() selects from BILLING_PROVIDER env, default StubProvider (33-37). StubProvider.createCheckout returns ok:false + COMING_SOON='/dashboard?upgrade=coming-soon', portalUrl null, handleWebhook {ok:false} (16-30). Never writes an entitlement. Offline: test_provider_switch.py GREEN.",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-40",
    "area": "control-plane",
    "feature": "App dashboard reads: myUsage / myUsageTimeline / currentUser",
    "user_story": "As a user, I read my own usage gauge, activity timeline, and profile so that the dashboard shows only my tenant data.",
    "expected_behavior": "All requireUser-gated, index-only on the JWT userId. myUsage (re-exported via app/usage) returns the entitlement snapshot (tier,status,msgUsed,msgQuota,remaining with -1 unlimited,unlimited,periodStart/End) or null. myUsageTimeline returns the last n (clamped [1,100], default 30) usageEvents by_user_at desc as {at,lane,channel,units}. currentUser returns the caller's own users fields (email,displayName,name,image,tier,isOperator,createdAt) or null.",
    "source_refs": "control-plane/convex/app/billing.ts:16-82; control-plane/convex/app/usage.ts:1-7; control-plane/convex/app/account.ts:10-40",
    "verify_method": "convex_e2e/Playwright: authenticated calls return only the caller's entitlement/usage/profile; an unauthenticated call throws via requireUser.",
    "notes": "All three dashboard reads requireUser-gated + index-only. Live proof blocked.",
    "p2_test_status": "pass",
    "p2_evidence": "Read app/billing.ts:16-82, app/usage.ts:1-7, app/account.ts:10-40. myUsage requireUser-gated, entitlements.by_user, returns tier/status/msgUsed/msgQuota/remaining(-1 unlimited)/unlimited/period or null (16-54). myUsageTimeline requireUser-gated, by_user_at desc, limit clamp [1,100] default 30 (57-82). currentUser requireUser-gated, returns own users fields (10-40). app/usage re-exports myUsage/myUsageTimeline from billing (single source). All index-only on JWT userId. Live tenant-scoping + unauth-throw assertion blocked (seam/Playwright-auth).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-41",
    "area": "control-plane",
    "feature": "App channel connect: myChannels / createPairingToken / disconnectChannel",
    "user_story": "As a user, I list my channels, mint a pairing deep-link/code, and disconnect a channel so that I manage my own bindings and only ever touch my own.",
    "expected_behavior": "All requireUser-gated, tenant-scoped via channelBindings.by_user. myChannels returns the caller's bindings ({kind,address,verified,lastInboundAt,createdAt}). createPairingToken mints via issuePairingTokenImpl (one transaction) and returns {token,code,expiresAt,channel,deepLink}: a Telegram t.me/<TELEGRAM_BOT_USERNAME>?start=<token> link when the bot username is configured, else null (iMessage uses the code). disconnectChannel re-checks ownership after by_address lookup and deletes only the caller's binding (foreign/missing -> ok:false, idempotent).",
    "source_refs": "control-plane/convex/app/channels.ts:17-89",
    "verify_method": "convex_e2e/Playwright: authenticated createPairingToken('telegram') returns a deep-link; redeem via the testing seam flips myChannels to verified; disconnectChannel removes it; disconnecting a foreign address returns ok:false.",
    "notes": "Tenant-scoped channel mgmt + ownership re-check confirmed; offline channels test green. Live reactive-flip proof blocked (seam).",
    "p2_test_status": "pass",
    "p2_evidence": "Read app/channels.ts:17-89. All requireUser-gated, channelBindings.by_user scoped. myChannels returns caller's bindings {kind,address,verified,lastInboundAt,createdAt} (17-42). createPairingToken mints via issuePairingTokenImpl (one txn) and returns {token,code,expiresAt,channel,deepLink}: Telegram t.me/<TELEGRAM_BOT_USERNAME>?start=<token> when bot username configured else null (48-71). disconnectChannel re-checks ownership (binding.userId!==userId -> ok:false), deletes own binding (75-89). Offline: test_channels.py GREEN. Live redeem->verified flip = convex_e2e (blocked).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-42",
    "area": "control-plane",
    "feature": "Admin grantTier (manual paid grant)",
    "user_story": "As the operator, I manually grant a paid tier (WORKER_SECRET) so that I can test paid plans before the billing provider is live.",
    "expected_behavior": "admin:grantTier is WORKER_SECRET-gated (NOT under app/, never getAuthUserId). It writes an active entitlement for any userId at the given tier (optional quotaOverride) through the single writer writeEntitlement (source='stub'). Returns null.",
    "source_refs": "control-plane/convex/admin.ts:13-32",
    "verify_method": "convex_e2e: seed a throwaway tenant, grantTier(userId,'pro') with the worker secret, then myUsage/checkAndReserve reflect pro quota. Wrong secret errors.",
    "notes": "Privileged worker-gated manual grant via single writer confirmed. Live proof blocked.",
    "p2_test_status": "pass",
    "p2_evidence": "Read admin.ts:13-32. grantTier is WORKER_SECRET-gated (assertWorker first, 21), NOT under app/, never getAuthUserId. Writes an active entitlement for any userId at the given tier (optional quotaOverride) through the single writer writeEntitlement(source='stub') (23-29). Returns null. Live grant + myUsage-reflects assertion blocked (seam disabled).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-43",
    "area": "control-plane",
    "feature": "Test seam (testing.ts, double-gated)",
    "user_story": "As the test harness, I seed and drive real tenants over HTTP so that tenant isolation and idempotency are proven against live indexes, and the seam is inert in prod.",
    "expected_behavior": "Every mutation is DOUBLE-GATED: assertWorker(workerSecret) AND assertTestMode() (throws unless ALLOW_TEST_SEED==='1'; prod never sets it). Provides seedTwoTenants (two tt-*@test.invalid users + queued messages), testEnqueue (drives enqueueImpl), testIssue/RedeemTelegram/RedeemImessage/ExpirePairing, testSetEntitlement, testReapIdle/testBackdateInstance, and cleanupTenants (deletes ONLY tt-*@test.invalid users + their rows — a foreign/operator id is skipped).",
    "source_refs": "control-plane/convex/testing.ts:26-293",
    "verify_method": "convex_e2e (RUN_CONVEX_E2E=1, dev with ALLOW_TEST_SEED=1): the test_e2e_fleet_*.py / test_pairing.py / test_entitlement_quota.py suites use these seams; static reasoning confirms ALLOW_TEST_SEED gating + the tt-* cleanup safety guard.",
    "notes": "Test seam disabled (ALLOW_TEST_SEED unset on the live deployment — must not enable, prod-config mutation). The seam's own gating is statically correct; its live function is intentionally unavailable here.",
    "p2_test_status": "blocked",
    "p2_evidence": "Read testing.ts:26-293. assertTestMode() throws unless process.env.ALLOW_TEST_SEED==='1' (26-30). Grep: 10 exported functions, 11 assertWorker + 11 assertTestMode calls — every public mutation is DOUBLE-GATED (seedTwoTenants/testEnqueue/testIssue/Redeem*/ExpirePairing/testSetEntitlement/testReapIdle/testBackdateInstance/cleanupTenants). cleanupTenants restricted to /^tt-.*@test\\.invalid$/ (250). BLOCKED for live exercise: ALLOW_TEST_SEED is unset on dev:zany-tapir-501 (env constraint) so the seam is DISABLED — its consumers (the @convex_e2e suites) auto-skip. The double-gate + tt-* safety guard are statically confirmed; inert-in-prod behavior is exactly what disables it here.",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-44",
    "area": "control-plane",
    "feature": "Operator backfill migration + audit helpers",
    "user_story": "As the operator, I become a first-class tenant via a one-off migration so that my live path keeps working while my data gets userId/channel/replyTarget.",
    "expected_behavior": "backfillOperator (internalMutation, deploy-key gated via npx convex run): find-or-create the operator user (by OPERATOR_EMAIL, force isOperator+max), write a max+unlimited entitlement, upsert a verified (imessage, ALLOWED_USER_NUMBER) binding, tag existing operator messages/connectIntents with the additive columns, delete '+test*' stray intents, and recount missing-userId (expect 0). Idempotent (only patches rows still missing columns). countMessagesMissingUserId and operatorBindingStatus are internalQuery audit helpers.",
    "source_refs": "control-plane/convex/migrations.ts:28-188",
    "verify_method": "Static reasoning on idempotency (re-run patches nothing new). On dev: `npx convex run migrations:backfillOperator` then internal countMessagesMissingUserId returns missing:0 and operatorBindingStatus returns verifiedImessageBindings:1. Not in the checklist but verified as the operator-cutover mechanism.",
    "notes": "Not enumerated in the checklist but present and load-bearing; included for completeness | Idempotent backfill + audit helpers confirmed statically. Live run deferred (one-off operator-cutover mutation, out of scope).",
    "p2_test_status": "pass",
    "p2_evidence": "Read migrations.ts:28-188. backfillOperator internalMutation (deploy-key gated via npx convex run): find-or-create operator by OPERATOR_EMAIL forcing isOperator+max (43-62), max+unlimited entitlement via writeEntitlement quotaOverride=-1 (64-70), upsert verified (imessage,ALLOWED_USER_NUMBER) binding (73-93), tag existing operator messages userId/channel/replyTarget only when undefined (96-110), patch operator connectIntents + delete '+test*' strays (114-131), recount missingAfter (134-140). Idempotent (every write is conditional on missing field / existence). Audit helpers countMessagesMissingUserId + operatorBindingStatus (154-188). Live npx convex run on dev not executed (operator-cutover write).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "CP-45",
    "area": "control-plane",
    "feature": "Cron schedule registry",
    "user_story": "As the system, scheduled housekeeping runs on fixed intervals so that periods roll, stuck messages requeue, and stale tokens expire — while idle reaping stays disabled.",
    "expected_behavior": "crons.ts registers three active interval jobs: rollExpiredPeriods (1 hour), reapStaleProcessing (2 minutes), expireStaleTokens (30 minutes), each targeting a paginated self-rescheduling internalMutation. The reapIdleInstances job is intentionally COMMENTED OUT (all tiers stay warm 24/7) — its logic remains callable manually.",
    "source_refs": "control-plane/convex/crons.ts:10-50",
    "verify_method": "Static reasoning: confirm exactly three crons.interval registrations and that no reapIdle schedule is active. The targeted mutations are verified individually (CP-16, CP-21, CP-35).",
    "notes": "Exactly 3 active crons, reapIdle disabled — matches expected (all tiers warm 24/7).",
    "p2_test_status": "pass",
    "p2_evidence": "Read crons.ts:10-50. Exactly THREE crons.interval registrations: 'roll expired entitlement periods' {hours:1} -> internal.billing.grant.rollExpiredPeriods (14-19); 'reap stale processing messages' {minutes:2} -> internal.messages.reapStaleProcessing (24-29); 'expire stale pairing tokens' {minutes:30} -> internal.pairing.expireStaleTokens (43-48). The reapIdleInstances job is a COMMENTED prose block (31-39), NO active schedule (its logic stays callable manually). Each target is a paginated self-rescheduling internalMutation (verified in CP-16/21/35).",
    "error_found": null,
    "severity": null,
    "error_type": null,
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-01",
    "area": "assistant",
    "feature": "Fast lane (direct MiniMax, no tools, reasoning-off)",
    "user_story": "As a user texting the assistant, I can send chit-chat or a quick question and expect a reply in ~1.5-2.7s instead of waiting for the full agent to cold-start.",
    "expected_behavior": "When FAST_LANE_ENABLED is on, the message has no URL, and a MiniMax key is present, the worker makes ONE direct chat-completion call with reasoning disabled (thinking:{type:disabled} on native MiniMax, reasoning:{enabled:false} on OpenRouter, selected by base_url). A stage-1 router prompt either answers directly or emits a sentinel. Any transport/model error returns None (defers to Hermes), never raises; an empty message or missing api_key raises ValueError.",
    "source_refs": "lab/skeleton/fast_lane.py:215-270; lab/skeleton/fast_lane.py:163-212; lab/skeleton/worker.py:381-393; lab/skeleton/worker.py:432-452",
    "verify_method": "pytest lab/tests/test_fast_lane.py (fake requests.Session asserts reasoning-off body, answer/defer routing, fail-safe None on error).",
    "notes": "Covered by test_fast_lane.py; gating in worker covered by test_worker_fast_lane.py / test_worker_routing.py | Worker gating also green: test_worker_fast_lane.py + test_worker_routing.py exit 0.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_fast_lane.py green (exit 0). Asserts provider-aware reasoning-off body: test_native_minimax_uses_thinking_kill_switch -> body['thinking']=={'type':'disabled'} and 'reasoning' not in body; test_openrouter_uses_reasoning_enabled_false; answer/defer routing and fail-safe None on transport error all asserted via FakeSession.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-02",
    "area": "assistant",
    "feature": "Stage-1 three-way router (answer / [[THINK]] / [[DEFER]])",
    "user_story": "As a user, I can send anything and expect it routed to the cheapest correct lane: answered now, escalated to reasoning, or handed to the full agent.",
    "expected_behavior": "The stage-1 probe (reasoning OFF) returns a direct answer, or exactly [[THINK]] (hard reasoning, no tools) or [[DEFER]] (needs real-time info, the user's private data, or an action). [[DEFER]] or a None probe defers to Hermes; [[THINK]] runs the medium lane (or defers if medium disabled). The router is strongly biased toward ANSWER since a wrong defer is only slower while a wrong direct answer on a tool-need is a capability regression.",
    "source_refs": "lab/skeleton/fast_lane.py:65-94; lab/skeleton/fast_lane.py:251-270",
    "verify_method": "pytest lab/tests/test_fast_lane.py and lab/tests/test_medium_lane.py (assert [[THINK]]/[[DEFER]] sentinels route to medium/defer).",
    "notes": "Covered by test_fast_lane.py + test_medium_lane.py | Answer-bias is a prompt property; sentinel routing is hermetically asserted.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_fast_lane.py + test_medium_lane.py green (exit 0). Three-way router sentinels [[THINK]]/[[DEFER]] asserted to route to medium/defer; None probe defers to Hermes.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-03",
    "area": "assistant",
    "feature": "Medium lane ([[THINK]] -> reasoning-on, no tools)",
    "user_story": "As a user asking a genuinely hard but tool-free question, I can expect a careful, correct answer faster than the full agent (no cold-start).",
    "expected_behavior": "On a [[THINK]] verdict, when MEDIUM_LANE_ENABLED is on, a second MiniMax-M3 call runs with reasoning ON (no tools), a larger timeout (~25s) and token budget (~2048). It returns the cleaned answer, or None to defer to Hermes if the model bails with a sentinel or answers with a capability refusal. Emitted <think> blocks are stripped from content.",
    "source_refs": "lab/skeleton/fast_lane.py:96-107; lab/skeleton/fast_lane.py:273-292; lab/skeleton/worker.py:236-239",
    "verify_method": "pytest lab/tests/test_medium_lane.py (fake session: [[THINK]] then a thinking-on answer; assert second call omits reasoning-off and strips <think>).",
    "notes": "Covered by test_medium_lane.py.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_medium_lane.py green (exit 0). Fake session: [[THINK]] then thinking-on answer; asserts the 2nd call omits reasoning-off and strips <think> blocks; sentinel/refusal -> None defer.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-04",
    "area": "assistant",
    "feature": "Heavy/Hermes lane (full tools + browser)",
    "user_story": "As a user whose request needs tools, the web, or my private data, I can expect the full agent to do the work and reply, even if it takes 20-40s.",
    "expected_behavior": "When no lane answered, the worker re-pins the operator LLM provider then invokes headless `hermes chat --quiet --query=<msg>` as a subprocess with HERMES_HOME set; the untrusted message is fused into a single --query= token so embedded --flags can't be parsed as argv. A nonzero exit raises RuntimeError (meaningful in --quiet); the cleaned stdout is returned, or '(no reply)'.",
    "source_refs": "lab/skeleton/hermes_bridge.py:92-145; lab/skeleton/worker.py:989-1017",
    "verify_method": "pytest lab/tests/test_hermes_bridge.py (mocks subprocess.run; asserts single --query= token, exit-code handling, output cleaning).",
    "notes": "Covered by test_hermes_bridge.py; live model call is gated/separate | Live model call is gated/separate (out of scope); subprocess contract fully covered hermetically.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_hermes_bridge.py green (exit 0). Mocks subprocess.run; asserts single fused --query= token (argv flag-smuggling defense), nonzero-exit -> RuntimeError, cleaned stdout or '(no reply)'.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-05",
    "area": "assistant",
    "feature": "Capability-refusal safety net (_REFUSAL)",
    "user_story": "As a user, when the model 'answers' by admitting it lacks access or real-time data, I can expect it to silently hand off to the real agent instead of replying with an excuse.",
    "expected_behavior": "A stage-1 or medium-lane answer that matches the _REFUSAL regex (no access / real-time data / need a connection, RU+EN, anchored to avoid idioms) is treated as a deferral and returns None so Hermes actually does the task. In the streaming path a refusal-only first output defers with nothing sent; a contradictory later refusal (after real bubbles) is skipped.",
    "source_refs": "lab/skeleton/fast_lane.py:109-124; lab/skeleton/fast_lane.py:267-270; lab/skeleton/fast_lane.py:453-473",
    "verify_method": "pytest lab/tests/test_fast_lane.py and lab/tests/test_stream_fast_lane.py (assert refusal-text answer -> None / deferred=True).",
    "notes": "Covered by test_fast_lane.py + test_stream_fast_lane.py.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_fast_lane.py + test_stream_fast_lane.py green (exit 0). Refusal-text answer (_REFUSAL regex, RU+EN) -> None / deferred=True; streaming refusal-only first output defers with nothing sent.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-06",
    "area": "assistant",
    "feature": "URL prefilter (skip fast lane on links)",
    "user_story": "As a user sharing a link, I can expect it to go straight to the real tool agent (saved-content/browser/fetch) rather than paying a fast-lane round-trip first.",
    "expected_behavior": "contains_url() matches http(s) URLs, bare www., and bare domains on a known TLD list. When true, _fast_lane_allowed returns False so the message bypasses the fast lane entirely and is handled by Hermes.",
    "source_refs": "lab/skeleton/fast_lane.py:56-63; lab/skeleton/fast_lane.py:137-139; lab/skeleton/worker.py:387-393",
    "verify_method": "pytest lab/tests/test_fast_lane.py (contains_url cases) and lab/tests/test_worker_routing.py (a URL message defers to run_fn).",
    "notes": "Covered by test_fast_lane.py.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_fast_lane.py green (exit 0) covers contains_url cases (http(s), bare www., bare domain on known TLD); test_worker_routing.py green confirms a URL message defers to run_fn (bypasses fast lane).",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-07",
    "area": "assistant",
    "feature": "Streaming fast lane (Poke-style incremental bubbles)",
    "user_story": "As a user, I can see the assistant's reply arrive paragraph-by-paragraph and feel a fast first response instead of staring at nothing.",
    "expected_behavior": "When STREAMING_ENABLED + multi-bubble are on and the channel is iMessage, stage-1 streams via SSE: a [[DEFER]]/[[THINK]] verdict is detected from the first tokens (<1s), each closed-paragraph bubble is emitted as it completes within the bubble budget (overflow coalesced into one final bubble), and the StreamResult records reply/emitted/deferred/errored. A transport error with nothing emitted falls back to a cheap non-streaming retry. Telegram does NOT stream (one batch message).",
    "source_refs": "lab/skeleton/fast_lane.py:368-549; lab/skeleton/worker.py:671-680; lab/skeleton/worker.py:964-981",
    "verify_method": "pytest lab/tests/test_stream_fast_lane.py (fake SSE iter_lines: direct answer, multi-paragraph early emit, defer/think, refusal, HTTP error, deadline).",
    "notes": "Covered by test_stream_fast_lane.py; worker wiring in test_worker_fast_lane.py / test_worker_multibubble_typing.py | Telegram-no-stream invariant exercised in worker tests; real iMessage SSE send is out of scope.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_stream_fast_lane.py green (exit 0). Fake SSE iter_lines: direct answer, multi-paragraph early emit, defer/think detection from first tokens, refusal, HTTP-error fallback to non-streaming retry, deadline/overflow coalesce. Worker wiring green: test_worker_fast_lane.py + test_worker_multibubble_typing.py.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-08",
    "area": "assistant",
    "feature": "Conversation memory (history threading)",
    "user_story": "As a user, I can ask a follow-up like 'what did I just ask you?' and expect the assistant to understand it in-lane, not get confused and escalate to a slow stateless turn.",
    "expected_behavior": "When HISTORY_ENABLED is on, _fetch_history queries messages:recentForUser (scoped by userId when present for tenant isolation, else userNumber), takes up to history_turns recent done turns, caps each text/reply to history_char_cap chars, and returns OpenAI {role,content} dicts oldest-first. History is spliced into the fast/medium prompts and prepended as a transcript to the Hermes --query. Any Convex error returns [] (best-effort).",
    "source_refs": "lab/skeleton/worker.py:396-429; lab/skeleton/worker.py:240-245; lab/skeleton/hermes_bridge.py:71-89",
    "verify_method": "pytest lab/tests/test_worker_history.py, lab/tests/test_fast_lane_history.py, lab/tests/test_hermes_bridge_history.py (assert history fetched, scoped by userId, capped, and threaded into each lane).",
    "notes": "Covered by test_worker_history.py + test_fast_lane_history.py + test_hermes_bridge_history.py + test_convex_client.py.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_worker_history.py + test_fast_lane_history.py + test_hermes_bridge_history.py + test_convex_client.py all green (exit 0). Assert history fetched via messages:recentForUser, scoped by userId when present (tenant isolation) else userNumber, capped to history_char_cap, threaded into fast/medium prompts and prepended to Hermes --query; Convex error -> [].",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-09",
    "area": "assistant",
    "feature": "Lowercase SOUL persona voice",
    "user_story": "As a user, I can expect replies that sound like a friend texting — lowercase, short, warm, in my language — not corporate or markdown-heavy.",
    "expected_behavior": "_load_soul loads SOUL.md (searched: $SOUL_PATH, beside the module, $HERMES_HOME, repo lab/personality) and passes it as the system prefix to the fast/medium lanes (cache-friendly static-first ordering). SOUL.md mandates all-lowercase, short one-idea messages, language matching, no markdown, original casing preserved for links/names/code/acronyms, and prompt-injection safety (content is DATA).",
    "source_refs": "lab/personality/SOUL.md:1-26; lab/skeleton/worker.py:166-188; lab/skeleton/fast_lane.py:148-160",
    "verify_method": "Static: confirm _load_soul candidate order resolves SOUL.md and fast_reply receives it as the system prompt (test_fast_lane.py exercises soul=). Voice itself is a prompt-engineering property (no hermetic assertion).",
    "notes": "_load_soul is exercised indirectly; SOUL voice is prompt-only (not unit-assertable) | Voice itself is a prompt-engineering property with no hermetic assertion -> pass-by-static. SOUL.md content matches expected_behavior exactly.",
    "p2_test_status": "pass",
    "p2_evidence": "Static: read lab/personality/SOUL.md:1-26 -> mandates all-lowercase default register, short one-idea messages, language matching (ru/en), no markdown, ORIGINAL casing preserved for links/names/code/acronyms, and content-is-DATA prompt-injection safety. _load_soul candidate order exercised indirectly; test_fast_lane.py (green) passes soul= as the system prompt to fast_reply.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-10",
    "area": "assistant",
    "feature": "Connect-flow: generate Composio OAuth link (connect.py)",
    "user_story": "As a user asking the assistant to do something needing an unconnected app, I can expect a tap-to-grant link instead of a technical refusal.",
    "expected_behavior": "connect.py CLI takes a toolkit slug (+ optional --user-id, default COMPOSIO_USER_ID) and calls ComposioClient.create_link, printing JSON {ok:true, toolkit, ...redirect_url} on success or {ok:false, error} (exit 1) on failure. SOUL instructs the agent to send the link via the connections skill.",
    "source_refs": "lab/skills/connections/scripts/connect.py:1-23; lab/personality/SOUL.md:19-22",
    "verify_method": "pytest lab/tests/test_connect.py (fake ComposioClient; assert JSON shape and exit codes).",
    "notes": "Covered by test_connect.py + test_composio_api.py.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_connect.py + test_composio_api.py green (exit 0). Fake ComposioClient asserts JSON {ok:true, toolkit, ...redirect_url} on success and {ok:false,error} exit 1 on failure for connect.py.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-11",
    "area": "assistant",
    "feature": "Connect-flow: status + intent recording (conn_status.py, pending.py)",
    "user_story": "As a user, after I tap a connect link, I can expect the assistant to remember the task I asked for and resume it automatically once the connection goes live.",
    "expected_behavior": "conn_status.py reports a toolkit's Composio connection_status (ACTIVE/etc) as JSON. pending.py 'add' records a connect-intent in Convex (intents:addIntent) with the verbatim task, required toolkit slugs, and user id (raises if no user id) so the worker can auto-resume; both print {ok,...} and exit 0/1.",
    "source_refs": "lab/skills/connections/scripts/conn_status.py:1-23; lab/skills/connections/scripts/pending.py:20-48",
    "verify_method": "pytest lab/tests/test_conn_status.py and lab/tests/test_pending.py (fake clients; assert JSON + Convex mutation args).",
    "notes": "Covered by test_conn_status.py + test_pending.py.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_conn_status.py + test_pending.py green (exit 0). Assert conn_status JSON shape and pending.py 'add' Convex intents:addIntent mutation args (verbatim task, toolkit slugs, user id; raises without user id).",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-12",
    "area": "assistant",
    "feature": "Auto-resume intent polling (process_intents)",
    "user_story": "As a user, I can connect an app whenever I get around to it and expect the assistant to finish my original task on its own, without me asking again.",
    "expected_behavior": "On a wall-clock cadence (intent_interval, default 10s), process_intents lists pending intents, expires any older than intent_ttl (1h) via intents:expireIntent, polls Composio connection_status for each required toolkit, and calls intents:resolveIntent with the now-active toolkits (which enqueues a synthetic resume turn). Never raises — any error is logged so the loop survives. No-op if ComposioClient or COMPOSIO_API_KEY is absent.",
    "source_refs": "lab/skeleton/worker.py:1045-1076; lab/skeleton/worker.py:1124-1129",
    "verify_method": "pytest lab/tests/test_worker.py (process_intents with fake convex+composio: assert expire on TTL, resolveIntent with active toolkits, never raises).",
    "notes": "Covered by test_worker.py (process_intents cases).",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_worker.py green (exit 0). process_intents with fake convex+composio: asserts expire on TTL (intents:expireIntent), resolveIntent with active toolkits, no-op without ComposioClient/key, never raises.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-13",
    "area": "assistant",
    "feature": "Rich-message directive parsing (channels/rich.py)",
    "user_story": "As a user, I can receive reactions, images, and polls when the assistant's reply contains them, without the brain learning a new output protocol.",
    "expected_behavior": "parse_rich turns a flat reply into ordered RichParts: leading [[react:EMOJI]] tokens -> ReactionPart(s); a ```poll fence (question + options) -> PollPart; markdown ![](url) or a bare http(s) URL ending in an image extension -> ImagePart (non-image URLs and markdown links stay opaque text); everything else merges into TextPart. A reply with no directives yields a single TextPart (byte-identical to the plain send path).",
    "source_refs": "lab/skeleton/channels/rich.py:179-216; lab/skeleton/channels/rich.py:83-97; lab/skeleton/channels/rich.py:100-154",
    "verify_method": "pytest lab/tests/test_rich.py (assert reaction/poll/image/text ordering, link-not-shredded, no-directive single TextPart).",
    "notes": "Covered by test_rich.py; worker integration in test_worker_rich.py.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_rich.py green (exit 0). Assert leading [[react:]] -> ReactionPart(s), ```poll fence -> PollPart, ![](url)/bare image-ext URL -> ImagePart, non-image URLs + markdown links stay opaque text, no-directive reply -> single TextPart. Worker integration green in test_worker_rich.py (run by full suite).",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-14",
    "area": "assistant",
    "feature": "Sendblue iMessage channel (text/media/voice/reaction/poll-degrade)",
    "user_story": "As an iMessage user, I can receive text, a real image/file/voice attachment, and tapback reactions, with polls degraded to a numbered list.",
    "expected_behavior": "SendblueChannel renders plain text, splits into Poke bubbles, and sends via SendblueClient. send_rich: TextPart->send_message; Image/File/Voice->send_message(media_url=) for http(s) srcs only (file-exfil guard), falling back to a text URL if the media POST fails; ReactionPart maps the emoji onto Sendblue's 6 tapbacks (POST /send-reaction targeting reply_to) or skips an unmappable one; PollPart degrades to a numbered text list. Every send is best-effort -> OutboundResult, never raises.",
    "source_refs": "lab/skeleton/channels/sendblue_channel.py:45-181; lab/skeleton/sendblue_client.py:42-131",
    "verify_method": "pytest lab/tests/test_sendblue_rich.py, lab/tests/test_channels.py, lab/tests/test_sendblue_client.py (fake client asserts media_url/reaction/poll-degrade and non-remote-src refusal).",
    "notes": "Covered by test_sendblue_rich.py + test_channels.py + test_sendblue_client.py + test_sendblue_typing.py | Real iMessage send out of scope; render/mapping/guard logic fully covered hermetically.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_sendblue_rich.py + test_channels.py + test_sendblue_client.py + test_sendblue_typing.py green (exit 0). Assert TextPart->send_message, Image/File/Voice->media_url for http(s) srcs only (non-remote-src refusal / file-exfil guard), reaction->6-tapback map (POST /send-reaction) or skip, poll-degrade to numbered list, every send best-effort never raises.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-15",
    "area": "assistant",
    "feature": "Photon iMessage channel + sidecar (rich via loopback)",
    "user_story": "As an operator, I can route rich iMessage through the Photon Spectrum sidecar (attachments/voice/reactions/typing) when IMESSAGE_PROVIDER=photon.",
    "expected_behavior": "PhotonChannel is an HTTP client to the vendored sidecar (bearer X-Hermes-Sidecar-Token): send_message->/send {format:markdown}, send_typing->/typing, send_rich maps Image/File/Voice->/send-attachment, ReactionPart->/react (needs reply_to else ok=False), PollPart->numbered text via /send. ensure_sidecar probes /healthz, reaps a stale listener, and spawns `node index.mjs`. ChannelRegistry.from_config builds it only when provider=='photon' AND base+project creds exist, else falls back to Sendblue (never an empty iMessage slot).",
    "source_refs": "lab/skeleton/channels/photon_channel.py:10-33; lab/skeleton/channels/photon_channel.py:95-120; lab/skeleton/channels/base.py:135-165",
    "verify_method": "pytest lab/tests/test_photon_channel.py (injected poster/head fakes assert route mapping, reaction-needs-reply_to, poll-degrade, MIME handling) and lab/tests/test_provider_switch.py (registry photon->sendblue fallback).",
    "notes": "Covered by test_photon_channel.py + test_provider_switch.py; real sidecar/iMessage send is a gated live test | Real sidecar/iMessage send is a gated live test (out of scope); HTTP-client contract covered hermetically. Photon is not the default provider (IMESSAGE_PROVIDER stays sendblue in prod).",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_photon_channel.py + test_provider_switch.py green (exit 0). Injected poster/head fakes assert route mapping (/send, /typing, /send-attachment, /react, poll-degrade), reaction-needs-reply_to, MIME handling; registry builds Photon only when provider=='photon' AND creds present, else falls back to Sendblue (never an empty iMessage slot).",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-16",
    "area": "assistant",
    "feature": "Telegram channel (sendRichMessage markdown, native polls/reactions)",
    "user_story": "As a Telegram user, I can receive richly-formatted replies, native polls, photos/files/voice, and reactions on my messages.",
    "expected_behavior": "TelegramChannel: in rich mode (default; kill-switch TELEGRAM_RICH_ENABLED=0) the main reply is sent as ONE sendRichMessage with the RAW reply as rich_message.markdown (render/split become pass-throughs); on any rich failure it falls back to classic render_html + sendMessage so the reply is never lost. send_rich maps Image->sendPhoto, File->sendDocument, Voice->sendVoice, Reaction->setMessageReaction(reply_to=message_id), Poll->sendPoll (native). All sends best-effort -> OutboundResult.",
    "source_refs": "lab/skeleton/channels/telegram_channel.py:50-171; lab/skeleton/channels/telegram_client.py:46-86",
    "verify_method": "pytest lab/tests/test_telegram_rich.py, lab/tests/test_telegram_rich_markdown.py, lab/tests/test_telegram_client.py, lab/tests/test_worker_telegram.py (fake client asserts rich->classic fallback, native poll/reaction, 400->plain + 429 retry).",
    "notes": "Covered by test_telegram_rich.py + test_telegram_rich_markdown.py + test_telegram_client.py + test_worker_telegram.py | Real Telegram send out of scope.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_telegram_rich.py + test_telegram_rich_markdown.py + test_telegram_client.py + test_worker_telegram.py green (exit 0). Assert rich-mode sendRichMessage with raw reply as rich_message.markdown, rich-failure -> classic render_html+sendMessage fallback (reply never lost), Image/File/Voice/Reaction/Poll native mapping, 400->plain + 429 retry.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-17",
    "area": "assistant",
    "feature": "Telegram outbound formatting (tg_format render_html / split)",
    "user_story": "As a Telegram user, I can receive replies whose markdown is converted to valid HTML and split within the 4096 limit without broken tags.",
    "expected_behavior": "render_html maps the model's light Markdown subset to Bot API HTML (**bold**->b, *italic*->i, `code`->code, fenced->pre/code), HTML-escaping prose and stashing code bodies behind NUL placeholders so tags inside code aren't interpreted. split_html_safe chunks rendered HTML to <=3800 chars on paragraph/line/tag-safe boundaries and closes+reopens tags spanning a boundary so each chunk is standalone-valid.",
    "source_refs": "lab/skeleton/channels/tg_format.py:40-85; lab/skeleton/channels/tg_format.py:103-166",
    "verify_method": "pytest lab/tests/test_tg_format.py (assert bold/italic/code mapping, NUL stripping, no-tag-cut split, tag balancing across chunks).",
    "notes": "Covered by test_tg_format.py; only used in Telegram classic (non-rich) mode | Only used in Telegram classic (non-rich) mode / fallback path.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_tg_format.py green (exit 0). Assert bold/italic/code mapping, fenced->pre/code, NUL placeholder stash (tags inside code not interpreted), split_html_safe no-tag-cut on paragraph/line/tag-safe boundaries, tag close+reopen balancing across chunks (<=3800 chars).",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-18",
    "area": "assistant",
    "feature": "Browser harness Lane B seed (BROWSER_HARNESS_ENABLED)",
    "user_story": "As an operator, I can have each fleet user's agent browse via the shared browser-harness (persistent per-user logins) instead of the built-in browser, with the built-in kept as a kill-switch fallback.",
    "expected_behavior": "seed_browser_harness (default ON; BROWSER_HARNESS_ENABLED=0 -> no-op) idempotently rewrites the tenant's config.yaml: sets platform_toolsets.cli to the default toolsets MINUS 'browser', allowlists BU_NAME/BU_CDP_URL/BU_CDP_WS through terminal.env_passthrough, re-pins the model block (writing a config short-circuits env model resolution), and copies the browser-harness SKILL.md into HERMES_HOME. Fail-soft: any error leaves the built-in browser in place.",
    "source_refs": "lab/skeleton/worker.py:1166-1170; lab/skeleton/worker.py:1218-1267; lab/skills/browser-harness/SKILL.md:11-46",
    "verify_method": "pytest lab/tests/test_browser_harness_seed.py (asserts cli list lacks 'browser', BU_* passthrough, skill seeded, kill-switch off=no-op, idempotent).",
    "notes": "Covered by test_browser_harness_seed.py; live in-container browse is a gated e2e | Live in-container browse is a gated e2e (out of scope).",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_browser_harness_seed.py green (exit 0). Asserts seed_browser_harness rewrites config.yaml: platform_toolsets.cli = defaults MINUS 'browser', BU_NAME/BU_CDP_URL/BU_CDP_WS via terminal.env_passthrough, model block re-pinned, SKILL.md copied; BROWSER_HARNESS_ENABLED=0 -> no-op; idempotent; fail-soft leaves built-in browser.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-19",
    "area": "assistant",
    "feature": "Sendblue inbound parsing (parse_inbound)",
    "user_story": "As the system, I can validate an inbound Sendblue webhook and ignore echoes/opt-outs/empty events before doing any work.",
    "expected_behavior": "parse_inbound returns None for a non-dict payload, an outbound echo (is_outbound true), an opted-out sender, or an empty text/number; otherwise it returns a frozen InboundMessage(text, user_number, handle, media_url, opted_out=False) with text/number trimmed and handle coerced to str. Never trusts external data.",
    "source_refs": "lab/skeleton/sendblue_client.py:134-167",
    "verify_method": "pytest lab/tests/test_parse_inbound.py (assert echo/opt-out/empty -> None and a valid event -> InboundMessage).",
    "notes": "Covered by test_parse_inbound.py.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_parse_inbound.py green (exit 0). Asserts non-dict / outbound-echo / opted-out / empty-text-or-number -> None; valid event -> frozen InboundMessage with trimmed text/number and str-coerced handle.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-20",
    "area": "assistant",
    "feature": "Photon inbound consumer (NDJSON gRPC stream)",
    "user_story": "As the system, I can consume rich iMessage inbound from the Photon sidecar stream, dedup replays, skip reactions, and enqueue real messages to the durable queue.",
    "expected_behavior": "PhotonInboundConsumer opens the sidecar /inbound NDJSON stream (bearer header) with capped exponential-backoff reconnect. Per line: parse JSON, require a string messageId, bounded-LRU dedup (gRPC at-least-once), map content (text/attachment->note+cached path/voice/group; reaction & unknown -> skip), resolve sender address (sender.id|space.phone|space.id), and enqueue via messages:enqueuePhotonInbound; unknown sender (no verified binding) is logged + skipped. handle_line never raises and returns an outcome tag.",
    "source_refs": "lab/skeleton/photon_inbound.py:76-128; lab/skeleton/photon_inbound.py:202-302",
    "verify_method": "pytest lab/tests/test_photon_inbound.py (producer<->consumer contract: real sidecar event shapes -> enqueue args, dedup, reaction-skip, unknown-sender drop, bad line no-raise).",
    "notes": "Covered by test_photon_inbound.py; only wired when IMESSAGE_PROVIDER=photon | Only wired when IMESSAGE_PROVIDER=photon (not prod default); NDJSON stream live consume is out of scope.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_photon_inbound.py green (exit 0). Producer<->consumer contract over real sidecar event shapes: enqueue args via messages:enqueuePhotonInbound, bounded-LRU dedup (at-least-once), reaction/unknown -> skip, unknown-sender (no verified binding) drop, bad line never raises (returns outcome tag).",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-21",
    "area": "assistant",
    "feature": "Single-user FastAPI webhook (app.py auth/dedup/concurrency)",
    "user_story": "As the system (legacy P1A skeleton), I can receive Sendblue inbound over HTTP with constant-time auth, idempotency, and a friendly error reply, never 5xx-ing back to Sendblue on processing errors.",
    "expected_behavior": "POST /sendblue/inbound/{token} compares the path token constant-time (hmac.compare_digest) and returns 401 before reading the body; invalid JSON/echo/opt-out/empty -> {ignored}; only the allowed operator number is acted on; empty handle is hard-rejected and a duplicate handle (bounded-LRU) -> {duplicate}; run_hermes runs via asyncio.to_thread under a Semaphore with an explicit timeout; any processing error -> friendly ERROR reply (never 5xx); the reply always goes to the config operator number.",
    "source_refs": "lab/skeleton/app.py:37-116; lab/skeleton/_dedup.py",
    "verify_method": "pytest lab/tests/test_app.py (FastAPI TestClient: bad token 401, echo/opt-out ignored, dedup, hermes error -> friendly reply, never 5xx).",
    "notes": "Covered by test_app.py. Legacy single-user bridge; superseded by the durable-queue worker (worker.py) in production | LEGACY P1A single-user FastAPI skeleton; superseded in production by the durable-queue worker (Convex http + worker.py). Not a user-facing bug. Invariants are still hermetically covered. Flag for Phase 3 only as a dead-path-removal candidate (not required).",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_app.py green (exit 0). FastAPI TestClient: bad path token -> 401 before body read (hmac.compare_digest), echo/opt-out/empty -> {ignored}, duplicate handle -> {duplicate}, hermes error -> friendly reply (never 5xx). Static: app.py is imported ONLY by skeleton/asgi.py (run via run.sh/uvicorn); grep of fleet/, control-plane/convex/, worker.py finds NO reference to app.py/asgi -> legacy/dead path, not the live inbound route.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-22",
    "area": "assistant",
    "feature": "Worker claim modes (shared / scoped / legacy) + per-message reply routing (A2)",
    "user_story": "As the system, I can run one shared bridge worker, a per-tenant scoped container, or the legacy operator worker, and always reply to the claimed message's own channel + address — never a hardcoded number.",
    "expected_behavior": "process_one selects the claim path by WORKER_MODE: 'shared'->claimNextAny (drains every tenant), scoped (USER_ID set)->claimNextForUser (server-side userId-scoped isolation), else legacy->claimNext. The reply is routed to the claimed row's own channel (ChannelRegistry.get) and replyTarget/userNumber; cfg.reply_target is a fallback ONLY for userId-less rows. A tenant row (userId set) with no reply address is marked failed (not leaked to the operator), and an unconfigured channel kind is marked failed rather than crashing the loop.",
    "source_refs": "lab/skeleton/worker.py:836-903; lab/skeleton/channels/base.py:84-104",
    "verify_method": "pytest lab/tests/test_worker_routing.py, lab/tests/test_claim_next_for_user.py, lab/tests/test_claimnext_any_skips_fleeted.py, lab/tests/test_regression_single_user.py, lab/tests/test_fleet_isolation.py (assert mode->claim fn and per-message routing).",
    "notes": "Covered by test_worker_routing.py + claim-mode + isolation tests | A2 per-message reply routing is the keystone fix (no hardcoded number); fully covered hermetically + by docker smoke.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_worker_routing.py + test_claim_next_for_user.py + test_claimnext_any_skips_fleeted.py + test_regression_single_user.py + test_fleet_isolation.py all green (exit 0). Assert WORKER_MODE -> claim fn (shared=claimNextAny, scoped=claimNextForUser, legacy=claimNext), reply routed to claimed row's own channel + replyTarget/userNumber, tenant row with no reply address marked failed (no operator leak), unconfigured channel marked failed not crash. Offline docker isolation smoke (Lead baseline) confirms alice->alice, bob->bob, no cross-tenant.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-23",
    "area": "assistant",
    "feature": "Multi-bubble splitting (bubbles.py, MULTI_BUBBLE_ENABLED)",
    "user_story": "As a user, I can receive a reply as a few short iMessage bubbles (like a person firing off quick texts) rather than one wall of text, and never lose content silently.",
    "expected_behavior": "split_into_bubbles splits on blank-line paragraphs (the model's intended breaks), splits an over-long paragraph on sentence boundaries then hard-slices a boundary-less blob; keeps the first max_bubbles (default 4) and marks the tail with '…' only when real content is dropped (caller logs it); every bubble clamped to hard_cap (1800). MULTI_BUBBLE_ENABLED=0 sends one chunk. URLs/code survive (sentence split only fires on punctuation+whitespace).",
    "source_refs": "lab/skeleton/bubbles.py:77-117; lab/skeleton/worker.py:246-254; lab/skeleton/worker.py:612-623",
    "verify_method": "pytest lab/tests/test_bubbles.py (paragraph/sentence/hard-slice cases, truncation marker, URL survival) and lab/tests/test_worker_multibubble_typing.py.",
    "notes": "Covered by test_bubbles.py + test_worker_multibubble_typing.py.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_bubbles.py + test_worker_multibubble_typing.py green (exit 0). Assert blank-line paragraph split, over-long paragraph sentence-boundary then hard-slice, keep first max_bubbles (4) + '…' marker only when real content dropped, hard_cap clamp (1800), URLs/code survive, MULTI_BUBBLE_ENABLED=0 -> one chunk.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-24",
    "area": "assistant",
    "feature": "Typing indicator (typing_indicator.py, TYPING_ENABLED)",
    "user_story": "As a user, I can see the '…' typing bubble within ~1s of my message and through a long turn, without it ever delaying my actual reply.",
    "expected_behavior": "TypingKeepalive fires channel.send_typing('start') on a daemon thread immediately and re-fires on a timer (interval, default 6s) inside max_duration_ms, then sends 'stop' when done. start()/poke() only set an Event and return instantly (the blocking ~0.6s network I/O is off the critical path); a lock guarantees no 'start' lands after 'stop'. Entirely best-effort: any error is swallowed at debug level. TYPING_ENABLED=0 or no target -> a no-op handle.",
    "source_refs": "lab/skeleton/typing_indicator.py:33-141; lab/skeleton/worker.py:480-536; lab/skeleton/worker.py:256-259",
    "verify_method": "pytest lab/tests/test_typing_indicator.py, lab/tests/test_sendblue_typing.py, lab/tests/test_worker_multibubble_typing.py (assert start/poke non-blocking, stop fires, no start-after-stop).",
    "notes": "Covered by test_typing_indicator.py + test_sendblue_typing.py + test_worker_multibubble_typing.py.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_typing_indicator.py + test_sendblue_typing.py + test_worker_multibubble_typing.py green (exit 0). Assert start()/poke() only set an Event and return instantly (blocking ~0.6s I/O off critical path), daemon-thread re-fire on interval, 'stop' fires when done, lock prevents start-after-stop, errors swallowed at debug, TYPING_ENABLED=0/no-target -> no-op handle.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-25",
    "area": "assistant",
    "feature": "Quota gate (M5: checkAndReserve / recordUsage / releaseReserve)",
    "user_story": "As a paying/free user, I can expect my real turns to be metered, get a friendly upsell when I hit my limit (with zero model spend), and never be locked out by a control-plane hiccup.",
    "expected_behavior": "When QUOTA_ENABLED and the row has a userId and isn't synthetic (resume:/e2e), the worker reserves one unit (quota:checkAndReserve) BEFORE any model call. A clean over-quota/canceled verdict -> friendly upsell text (with UPGRADE_URL if set) + messages:complete + NO lane (no spend). On success it records one usageEvents row; on a hard (undelivered) failure it releases the reserve (refund). A Convex error FAILS OPEN (proceeds unmetered, reserves nothing). Operator/legacy userId-less turns are unmetered.",
    "source_refs": "lab/skeleton/worker.py:911-937; lab/skeleton/worker.py:1021-1030; lab/skeleton/quota_client.py:25-104",
    "verify_method": "pytest lab/tests/test_worker_quota.py, lab/tests/test_quota_client.py, lab/tests/test_entitlement_quota.py (over-quota upsell+no-lane, allowed reserve+record, hard-fail refund, synthetic/legacy unmetered, Convex error fail-open).",
    "notes": "Covered by test_worker_quota.py + test_quota_client.py + test_entitlement_quota.py.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_worker_quota.py + test_quota_client.py + test_entitlement_quota.py green (exit 0). Assert reserve-before-model (quota:checkAndReserve), over-quota/canceled -> friendly upsell (UPGRADE_URL) + complete + NO lane (zero spend), success -> one usageEvents row, hard-fail -> releaseReserve refund, synthetic (resume:/e2e) + legacy userId-less unmetered, Convex error -> fail-open (proceeds unmetered).",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-26",
    "area": "assistant",
    "feature": "Graceful shutdown / SIGTERM drain + fleet desired=stopped (HEARTBEAT_ENABLED)",
    "user_story": "As an operator, when a container is stopped or the controller asks it to drain, I can expect any in-flight reply to finish instead of being killed mid-turn.",
    "expected_behavior": "SIGTERM/SIGINT (fail-soft install) set a module-level stop Event; run_loop checks should_stop() at the top, finishes the current process_one, stops claiming new work, and exits 0. In scoped mode the worker pings fleet:heartbeat once per loop iteration (HEARTBEAT_ENABLED, fail-soft); a desired='stopped' result logs a warning and begins the same graceful drain (does not hard-exit mid-iteration).",
    "source_refs": "lab/skeleton/worker.py:102-147; lab/skeleton/worker.py:787-805; lab/skeleton/worker.py:1115-1145; lab/skeleton/fleet_client.py:17-53",
    "verify_method": "pytest lab/tests/test_worker_shutdown.py and lab/tests/test_worker_heartbeat.py (SIGTERM sets flag -> drain+exit; desired=stopped -> drain; signal install fail-soft).",
    "notes": "Covered by test_worker_shutdown.py + test_worker_heartbeat.py.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_worker_shutdown.py + test_worker_heartbeat.py green (exit 0). Assert SIGTERM/SIGINT (fail-soft install) sets stop Event -> finishes current process_one, stops claiming, exits 0; scoped-mode fleet:heartbeat once/iteration (HEARTBEAT_ENABLED, fail-soft); desired='stopped' -> same graceful drain (no mid-iteration hard exit).",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-27",
    "area": "assistant",
    "feature": "Completion/failure mutations + friendly error reply (ERROR_REPLY)",
    "user_story": "As a user, I can expect every message to reach a terminal state and, on a failure, get a friendly note rather than leaking internal error text.",
    "expected_behavior": "Non-streaming: _complete_then_send marks messages:complete then sends; a complete failure degrades to messages:fail + the friendly ERROR_REPLY. Streaming: bubbles are already sent so _complete_after_send only records complete/fail (no second error reply). A Hermes hard failure marks fail, sends ERROR_REPLY, and flags hard_failed for a quota refund. Internal Hermes/subprocess text is never surfaced to the user.",
    "source_refs": "lab/skeleton/worker.py:150-152; lab/skeleton/worker.py:714-765; lab/skeleton/worker.py:1009-1017",
    "verify_method": "pytest lab/tests/test_worker.py and lab/tests/test_worker_review_fixes.py (assert complete-then-send, complete-failure->fail+ERROR_REPLY, streaming no double-error).",
    "notes": "Covered by test_worker.py + test_worker_review_fixes.py + test_worker_fast_lane.py.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_worker.py + test_worker_review_fixes.py green (exit 0; test_worker_fast_lane.py also green). Assert non-streaming _complete_then_send marks complete then sends, complete-failure -> messages:fail + friendly ERROR_REPLY, streaming _complete_after_send records complete/fail with no second error reply, Hermes hard failure -> fail + ERROR_REPLY + hard_failed flag for refund, internal text never surfaced.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-28",
    "area": "assistant",
    "feature": "Hermes output cleaning (_THINK/_ANSI/_APPROVAL/_NOTICE/_FRAME)",
    "user_story": "As a user, I can expect the agent's reply to be just the answer — never leaked reasoning, ANSI codes, approval prompts, warning notices, or box-drawing/session chrome.",
    "expected_behavior": "run_hermes strips, in order: <think> blocks, ANSI/CSI/OSC escapes, the whole DANGEROUS-COMMAND approval block (before _NOTICE so its header isn't orphaned), ⚠️ notice lines, and box-drawing/session_id frame lines (skill/terminal/browse chrome); then collapses 3+ newlines and trims, returning '(no reply)' if empty.",
    "source_refs": "lab/skeleton/hermes_bridge.py:37-68; lab/skeleton/hermes_bridge.py:139-145",
    "verify_method": "pytest lab/tests/test_hermes_bridge.py (assert each of think/ANSI/approval/notice/frame is stripped from sample stdout).",
    "notes": "Covered by test_hermes_bridge.py; ordering of _APPROVAL before _NOTICE is asserted.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_hermes_bridge.py green (exit 0). Assert run_hermes strips, in order, <think> blocks, ANSI/CSI/OSC, the DANGEROUS-COMMAND approval block (_APPROVAL before _NOTICE so header not orphaned), warning notice lines, box-drawing/session_id frame lines; collapses 3+ newlines, '(no reply)' if empty. _APPROVAL-before-_NOTICE ordering explicitly asserted.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-29",
    "area": "assistant",
    "feature": "Per-turn observability log (lane/bubbles/history/ttfb/dt) + adaptive polling",
    "user_story": "As an operator, I can read one structured log line per turn to see which lane answered, time-to-first-bubble, and end-to-end latency, and the worker polls fast during a conversation and backs off when idle.",
    "expected_behavior": "process_one logs: processed <id> lane=<fastlane|hermes|quota-denied> bubbles=N history=N in_chars=N hist=Xs ttfb=Xs dt=Xs, where ttfb is claim->first bubble on the wire (perceived latency) tracked by _BubbleEmitter.first_at. run_loop polls at poll_interval while active and backs off to idle_poll_interval after idle_after consecutive empty polls; a whole-iteration crash is logged and treated as an empty poll (worker backs off, never dies).",
    "source_refs": "lab/skeleton/worker.py:1032-1042; lab/skeleton/worker.py:561-583; lab/skeleton/worker.py:1146-1155",
    "verify_method": "Static reasoning + pytest lab/tests/test_worker_fast_lane.py / test_worker.py (assert lane label set, first_at tracked, adaptive interval selection in run_loop with max_iterations).",
    "notes": "ttfb/first_at asserted indirectly; adaptive cadence exercised via run_loop tests with injected sleep_fn | ttfb/first_at asserted indirectly; log format matches expected_behavior exactly. Pass by static + green indirect tests.",
    "p2_test_status": "pass",
    "p2_evidence": "Static: read worker.py:1033-1042 -> log line 'processed %s lane=%s bubbles=%d history=%d in_chars=%d hist=%.2fs ttfb=%s dt=%.2fs' (ttfb = first_at-started = claim->first bubble; worker.py:561/579-580 track _BubbleEmitter.first_at; worker.py:934 lane=quota-denied). Adaptive polling fields poll_interval/idle_poll_interval(3.0)/idle_after(6) at worker.py:215-219/318-325. test_worker_fast_lane.py + test_worker.py green exercise lane label + first_at + adaptive interval via injected sleep_fn/max_iterations.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "ASST-30",
    "area": "assistant",
    "feature": "Model re-pin security guard (_enforce_model_block)",
    "user_story": "As an operator, I can be sure a prompt-injected agent cannot persist a rogue LLM base_url to exfiltrate my prompts and API key.",
    "expected_behavior": "Before every Hermes turn, _enforce_model_block OVERWRITES config.yaml's model block (default/provider/base_url) from the MINIMAX_* env (OpenRouter slug+base when the key is sk-or-, else native MiniMax), stomping any agent-written model block. The API key is NEVER written to disk (stays in env, which a chat turn can't touch). No-op when the key is unset or the home dir is absent; fail-soft on a config hiccup.",
    "source_refs": "lab/skeleton/worker.py:1177-1215; lab/skeleton/worker.py:992-995",
    "verify_method": "pytest lab/tests/test_enforce_model_block.py (assert agent-supplied provider is stomped, key never written, OpenRouter-vs-native selection, no-op without key).",
    "notes": "Covered by test_enforce_model_block.py | Security guard against prompt-injection base_url exfiltration — fully covered hermetically.",
    "p2_test_status": "pass",
    "p2_evidence": "lab/tests/test_enforce_model_block.py green (exit 0). test_stomps_agent_supplied_provider_and_never_writes_key: an agent-written model block pointing base_url at attacker is OVERWRITTEN from MINIMAX_* env (out['model']['base_url']=='https://api.minimax.io/v1') and the API key is never written to config.yaml; OpenRouter (sk-or-) vs native selection and no-op-without-key also asserted.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "CTRL-01",
    "area": "fleet-controller",
    "feature": "Reconcile loop (poll + diff + act)",
    "user_story": "As the system, I can poll Convex for every per-user instance and drive Docker toward each one's desired state so that containers are launched, relaunched, stopped, or left alone without manual intervention.",
    "expected_behavior": "reconcile_once() calls convex.list_reconcile(), then for each instance computes _decide() and dispatches launch/relaunch/stop (noop and skip do nothing). A per-instance exception is caught and recorded as an {action:'error'} entry so one bad row never aborts processing of the others; it returns a list of action-summary dicts.",
    "source_refs": "fleet/controller/controller.py:473-504",
    "verify_method": "cd fleet/controller && python3 -m pytest -q tests/ (tests/test_controller.py::TestReconcileOnce — launch/stop/noop dispatch + test_reconcile_continues_on_per_instance_exception)",
    "notes": "Covered by TestReconcileOnce | Per-instance exception isolation verified; noop/skip correctly do nothing.",
    "p2_test_status": "pass",
    "p2_evidence": "fleet/controller tests green (82 passed). TestReconcileOnce -> 5 tests pass via `pytest -k TestReconcileOnce` incl. launch/relaunch/stop/noop dispatch + test_reconcile_continues_on_per_instance_exception. Read controller.py:473-504: per-instance try/except records {action:'error'} and continues; returns list of action-summary dicts.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "CTRL-02",
    "area": "fleet-controller",
    "feature": "Per-instance decision state machine",
    "user_story": "As the system, I can map an instance's (desired, status, containerId, heartbeatAt) to a single action so that reconcile behavior is deterministic and testable.",
    "expected_behavior": "_decide() returns 'launch' when desired=running and status in {provisioning,stopped,error} with no containerId; 'relaunch' when status=running but heartbeatAt is None or older than stale_ttl_s; 'noop' when running with a fresh heartbeat; 'stop' when desired=stopped and a containerId exists; 'noop' for desired=stopped with no container; and 'skip' (with a warning) for any unrecognized desired value.",
    "source_refs": "fleet/controller/controller.py:86-125",
    "verify_method": "cd fleet/controller && python3 -m pytest -q tests/ (tests/test_controller.py::TestDecide — all eight branches)",
    "notes": "stale_ttl computed in ms from now_fn()*1000; STALE_TTL_S default 90s (README/.env.example recommend 240s in prod) | Decision LOGIC is correct and fully tested. The STALE_TTL_S default-value discrepancy (90s code vs 240s recommended) is a config-default issue recorded as a FAIL on CTRL-13 (where the default is defined: config.py:145), not a logic bug here.",
    "p2_test_status": "pass",
    "p2_evidence": "TestDecide -> 8 tests pass (`pytest -k TestDecide`), all eight branches (launch/relaunch/noop-fresh/stop/noop-no-container/skip). Read controller.py:86-125 confirms the state machine. stale_ttl computed in ms from now_fn()*1000.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "CTRL-03",
    "area": "fleet-controller",
    "feature": "Container launch with per-tenant env",
    "user_story": "As the system, I can cold-start a per-user container with exactly the scoped env the worker requires so that each tenant's worker boots and claims only its own messages.",
    "expected_behavior": "_do_launch() picks a host via choose_host(), rehydrates GCS state, builds the env (WORKER_MODE=scoped, USER_ID, INSTANCE_ID, CONVEX_URL, WORKER_SECRET, HERMES_HOME=/data/tenants/<uid>, MINIMAX_*/derived endpoint, SENDBLUE_*, TELEGRAM_BOT_TOKEN, EXA_API_KEY, BU_* harness vars), docker-runs the image, then calls claimInstanceForLaunch. ALLOWED_USER_NUMBER is intentionally omitted; secret values come from the controller's own OS env and are never logged.",
    "source_refs": "fleet/controller/controller.py:209-273;fleet/controller/controller.py:287-360;fleet/controller/docker_driver.py:51-90",
    "verify_method": "cd fleet/controller && python3 -m pytest -q tests/ (tests/test_controller.py::TestDoLaunch::test_launch_calls_docker_run, test_launch_env_includes_user_id_and_scoped_mode, test_launch_env_excludes_allowed_user_number, test_launch_calls_claim_for_launch, test_launch_mounts_user_dir)",
    "notes": "Covered by TestDoLaunch | ALLOWED_USER_NUMBER intentional omission verified (multi-tenant keystone).",
    "p2_test_status": "pass",
    "p2_evidence": "TestDoLaunch -> 11 tests pass (`pytest -k TestDoLaunch`) incl. test_launch_calls_docker_run, test_launch_env_includes_user_id_and_scoped_mode, test_launch_env_excludes_allowed_user_number, test_launch_calls_claim_for_launch, test_launch_mounts_user_dir. Read controller.py:209-273 (_build_env) + 287-360 (_do_launch): WORKER_MODE=scoped, HERMES_HOME=/data/tenants/<uid>, ALLOWED_USER_NUMBER omitted, secrets via OS env never logged.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "CTRL-04",
    "area": "fleet-controller",
    "feature": "Boot-contract env-name correctness (Bug C1)",
    "user_story": "As an operator, I can trust that the env the controller injects matches exactly what the worker's WorkerConfig.from_env() reads so that launched containers do not crash on boot with a KeyError.",
    "expected_behavior": "_build_env() emits SENDBLUE_API_SECRET_KEY (not SENDBLUE_API_SECRET), SENDBLUE_FROM_NUMBER, and MINIMAX_BASE_URL with the precise names the worker bracket-reads. A missing controller-side var is logged as a warning but still passed through as empty.",
    "source_refs": "fleet/controller/controller.py:209-273",
    "verify_method": "cd fleet/controller && python3 -m pytest -q tests/ (tests/test_controller.py::TestBuildEnvBootsWorker — esp. test_built_env_constructs_worker_config_with_no_keyerror, which feeds _build_env output into the REAL lab/skeleton/worker.py WorkerConfig.from_env)",
    "notes": "Producer/consumer contract test; the keyerror test loads the actual worker module by path | Producer/consumer contract test loads the actual worker module by path — the right way to prove the boot contract.",
    "p2_test_status": "pass",
    "p2_evidence": "TestBuildEnvBootsWorker -> 7 tests pass (`pytest -k TestBuildEnvBootsWorker`) incl. test_built_env_constructs_worker_config_with_no_keyerror which feeds _build_env output into the REAL lab/skeleton/worker.py WorkerConfig.from_env. Read controller.py:236-255: emits SENDBLUE_API_SECRET_KEY (not _SECRET), SENDBLUE_FROM_NUMBER, MINIMAX_BASE_URL with exact bracket-read names; missing var -> warning + empty passthrough.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "CTRL-05",
    "area": "fleet-controller",
    "feature": "Model endpoint derived from key prefix (Bug C3)",
    "user_story": "As an operator, I can omit MINIMAX_BASE_URL/MODEL and still have every container hit the correct LLM endpoint so that a forgotten env var doesn't silently 401 every model call.",
    "expected_behavior": "_build_env() derives base_url+model from the MINIMAX_API_KEY prefix: an sk-or-* (OpenRouter) key yields https://openrouter.ai/api/v1 + minimax/minimax-m3, otherwise https://api.minimax.io/v1 + MiniMax-M3. An explicit MINIMAX_BASE_URL or MINIMAX_MODEL env value always overrides the derivation.",
    "source_refs": "fleet/controller/controller.py:221-234",
    "verify_method": "cd fleet/controller && python3 -m pytest -q tests/ (tests/test_controller.py::TestModelEndpointContract — openrouter/native derivation + explicit override)",
    "notes": "Covered by TestModelEndpointContract | Override-wins precedence verified in code (os.environ.get(...).strip() or derivation).",
    "p2_test_status": "pass",
    "p2_evidence": "TestModelEndpointContract -> 3 tests pass (`pytest -k TestModelEndpointContract`): openrouter derivation, native derivation, explicit override. Read controller.py:221-234: sk-or-* -> openrouter.ai/api/v1 + minimax/minimax-m3; else api.minimax.io/v1 + MiniMax-M3; explicit MINIMAX_BASE_URL/MINIMAX_MODEL env always wins.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "CTRL-06",
    "area": "fleet-controller",
    "feature": "Launch race resolution (claimInstanceForLaunch)",
    "user_story": "As the system, I can atomically claim a launch slot in Convex so that if two controllers race only one container survives and the duplicate is stopped.",
    "expected_behavior": "After docker run, _do_launch() calls convex.claim_for_launch(userId, host, containerId). If the claim raises, the container is docker-stopped and a launch_claim_error is returned; if claimed=False (race lost), the duplicate container is docker-stopped (launch_duplicate_stopped). On a successful claim the failure counter for the user is reset to 0.",
    "source_refs": "fleet/controller/controller.py:338-360;fleet/controller/convex_admin.py:58-77",
    "verify_method": "cd fleet/controller && python3 -m pytest -q tests/ (tests/test_controller.py::TestDoLaunch::test_launch_claim_false_stops_duplicate; conftest FakeConvexAdmin models the real 'running row cannot be re-claimed' guard)",
    "notes": "Covered by TestDoLaunch + conftest fake's status-aware claim guard | Race-loser duplicate stop + failure-counter reset verified.",
    "p2_test_status": "pass",
    "p2_evidence": "TestDoLaunch::test_launch_claim_false_stops_duplicate passes (in the 11-test TestDoLaunch set). Read controller.py:338-360: claim raises -> docker stop + launch_claim_error; claimed=False -> docker stop duplicate (launch_duplicate_stopped); success resets failure_counts[user]=0. conftest FakeConvexAdmin models the 'running row cannot be re-claimed' guard.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "CTRL-07",
    "area": "fleet-controller",
    "feature": "Stale/dead container relaunch with self-kill guard",
    "user_story": "As the system, I can detect a stale-heartbeat or dead container and restart it without the new container immediately killing itself so that an unhealthy tenant recovers instead of flapping.",
    "expected_behavior": "_do_relaunch() docker-stops the old container, then calls convex.mark_stopped() (best-effort, logged on failure) to transition the row OFF 'running' BEFORE reusing the launch path — otherwise claimInstanceForLaunch would return claimed=False for the still-'running' row and _do_launch would stop the brand-new container as a duplicate. markStopped leaves desired='running' so reconcile still wants it.",
    "source_refs": "fleet/controller/controller.py:363-405",
    "verify_method": "cd fleet/controller && python3 -m pytest -q tests/ (tests/test_controller.py::TestStaleRelaunch::test_relaunch_stops_then_starts — asserts exactly ONE docker stop and a claimed launch)",
    "notes": "Covered by TestStaleRelaunch; the single-stop assertion is the self-kill regression guard | Single-stop assertion proves the new container is not stopped as a duplicate.",
    "p2_test_status": "pass",
    "p2_evidence": "TestStaleRelaunch -> 1 test (test_relaunch_stops_then_starts) passes (`pytest -k TestStaleRelaunch`); asserts exactly ONE docker stop and a claimed launch — the self-kill regression guard. Read controller.py:363-405: stop old -> mark_stopped (best-effort) to move row OFF 'running' BEFORE reusing launch path; markStopped keeps desired='running'.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "CTRL-08",
    "area": "fleet-controller",
    "feature": "Crash-loop guard (Bug H1)",
    "user_story": "As an operator, I can rely on the controller to stop relaunching a container that dies before its first heartbeat so that a doomed tenant is marked error instead of burning the host every poll.",
    "expected_behavior": "_record_crash_relaunch() counts consecutive relaunches with no heartbeat advance; a fresher heartbeatAt than the previous tick resets the counter. After max_launch_failures crash-relaunches it calls convex.mark_error(), records the desired-marker (wantsAt, falling back to desired), and suppresses further launch/relaunch via _crash_block_active() until that marker changes (desired re-asserted), which clears the block and counters.",
    "source_refs": "fleet/controller/controller.py:134-198;fleet/controller/controller.py:293-302;fleet/controller/controller.py:378-391",
    "verify_method": "cd fleet/controller && python3 -m pytest -q tests/ (tests/test_controller.py::TestCrashLoopGuard — stops after max + marks error, no further docker run after block, healthy advance resets, block clears on desired re-assert)",
    "notes": "Distinct from the docker-run failure counter (failure_counts) in _do_launch; both gated by max_launch_failures | Distinct from failure_counts docker-run counter; both gated by max_launch_failures.",
    "p2_test_status": "pass",
    "p2_evidence": "TestCrashLoopGuard -> 4 tests pass (`pytest -k TestCrashLoopGuard`): stops after max + marks error, no further docker run after block, healthy heartbeat advance resets, block clears on desired re-assert. Read controller.py:134-198/293-302/378-391: _record_crash_relaunch counts no-heartbeat relaunches; mark_error after max; _crash_block_active suppresses until wantsAt/desired marker changes.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "CTRL-09",
    "area": "fleet-controller",
    "feature": "Graceful stop with state mirror (idle-reap path)",
    "user_story": "As the system, I can quiesce a container and persist its state before marking it stopped so that a deliberately stopped (e.g. reaped) tenant loses no data and restores on next launch.",
    "expected_behavior": "_do_stop() docker-stops the container FIRST (quiesce), THEN state_sync.mirror() pushes the bind-mount to GCS, then convex.mark_stopped() (best-effort, logged on failure). Stop-before-mirror ordering is required so the filesystem is safe to read.",
    "source_refs": "fleet/controller/controller.py:408-425;fleet/controller/convex_admin.py:79-81",
    "verify_method": "cd fleet/controller && python3 -m pytest -q tests/ (tests/test_controller.py::TestDoStop — test_stop_mirrors_state_after_stopping asserts stop precedes mirror; test_stop_calls_mark_stopped)",
    "notes": "Idle reaping itself is decided by Convex crons/desired-state; the controller only executes the resulting desired=stopped transition. Covered by TestDoStop | Idle-reap decision lives in Convex crons/desired-state; controller only executes the desired=stopped transition.",
    "p2_test_status": "pass",
    "p2_evidence": "TestDoStop -> 4 tests pass (`pytest -k TestDoStop`): test_stop_mirrors_state_after_stopping asserts stop precedes mirror; test_stop_calls_mark_stopped. Read controller.py:408-425: docker stop FIRST (quiesce) -> state_sync.mirror() -> convex.mark_stopped() best-effort. Stop-before-mirror ordering verified.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "CTRL-10",
    "area": "fleet-controller",
    "feature": "Immortal run loop, --once, and periodic mirror",
    "user_story": "As an operator, I can run the controller as a never-exiting service (or a single --once pass for smoke/health) so that transient Convex/Docker errors back off and retry instead of crashing the process.",
    "expected_behavior": "run() loads ControllerConfig.from_env() if not given, wires real ConvexAdminClient/DockerDriver/StateSync/SecretManager, and loops: reconcile_once with exponential backoff (1s doubling to 60s) on unhandled exceptions, sleeping poll_interval_s between passes. --once runs exactly one pass and returns. Every mirror_interval_s (0 disables) it runs _mirror_running_tenants() — a fail-soft GCS mirror of all running tenants for crash recovery.",
    "source_refs": "fleet/controller/controller.py:512-538;fleet/controller/controller.py:546-589",
    "verify_method": "cd fleet/controller && python3 -m pytest -q tests/ (tests/test_controller.py::TestRunLoop::test_run_loop_never_dies_on_reconcile_exception, test_run_once_flag_exits_after_single_pass; TestPeriodicMirror — only-running, fail-soft, default interval 300s)",
    "notes": "Covered by TestRunLoop + TestPeriodicMirror | Immortal loop + --once + periodic mirror all covered.",
    "p2_test_status": "pass",
    "p2_evidence": "TestRunLoop -> 2 tests (test_run_loop_never_dies_on_reconcile_exception, test_run_once_flag_exits_after_single_pass) + TestPeriodicMirror -> 4 tests (only-running, fail-soft, default interval 300s) all pass (`pytest -k 'TestRunLoop or TestPeriodicMirror'`). Read controller.py:512-538/546-589: run() wires real deps, exponential backoff 1s->60s, poll_interval_s sleep, --once single pass, _mirror_running_tenants every mirror_interval_s (0 disables).",
    "p3_fix_status": "n/a"
  },
  {
    "id": "CTRL-11",
    "area": "fleet-controller",
    "feature": "Placement / capacity bin-pack",
    "user_story": "As the system, I can place a new container on the least-loaded host that has RAM headroom and spare capacity so that hosts are bin-packed and a full fleet refuses new launches instead of overcommitting.",
    "expected_behavior": "choose_host() returns the eligible host with the fewest running containers (host-name tiebreak for determinism), where host_has_room() requires running<capacity and (100-ram_used_pct)>=headroom_pct; it returns None when all hosts are full. _build_hosts_state() derives per-host counts from docker.ps() and, lacking a real metrics agent, uses a fabricated ram_used_pct=40 and cfg.capacity_per_host (ponytail-marked); a None host yields launch_refused/hosts_full.",
    "source_refs": "fleet/controller/placement.py:18-67;fleet/controller/controller.py:304-310;fleet/controller/controller.py:433-465",
    "verify_method": "cd fleet/controller && python3 -m pytest -q tests/ (tests/test_placement.py — TestHostHasRoom/TestChooseHost/TestHostsFull; tests/test_controller.py::TestCapacityConfig for CAPACITY_PER_HOST env)",
    "notes": "adversarial=not_a_bug: documented ponytail ceiling (ram_used_pct=40), test_placement.py asserts the count-only gate; no fix needed.",
    "p2_test_status": "pass",
    "p2_evidence": "test_placement.py (TestHostHasRoom/TestChooseHost/TestHostsFull) + TestCapacityConfig -> 19 tests pass (`pytest -k 'TestHostHasRoom or TestChooseHost or TestHostsFull or TestCapacityConfig'`); placement math itself is correct. BUT read controller.py:454-465 (_build_hosts_state): ram_used_pct is HARDCODED to the constant 40 (ponytail-marked at :459-462) — there is no per-host /metrics agent, so the RAM-headroom branch in host_has_room (placement.py:38-39) can never refuse a host. Effective placement refuses ONLY on container count (capacity_per_host).",
    "error_found": "",
    "severity": "none",
    "error_type": "none",
    "p3_fix_status": "n/a"
  },
  {
    "id": "CTRL-12",
    "area": "fleet-controller",
    "feature": "GCS state sync (rehydrate/mirror) + docker/secret drivers",
    "user_story": "As the system, I can rehydrate per-user state before launch and mirror it after stop via injectable gcloud/docker/Secret-Manager drivers so that tenant logins/state persist across restarts and all I/O is fakeable in tests.",
    "expected_behavior": "StateSync.rehydrate() makedirs the bind-mount source (Bug C5: controller uid 10001 == container non-root uid) then gcloud storage rsync GCS->local; mirror() rsyncs local->GCS; both are fail-soft and exclude lock/WAL/journal/SingletonLock via one Python-regex --exclude (a glob would crash gcloud). DockerDriver wraps docker run/stop/inspect/ps/is_running with an injectable run_cmd. SecretManager.ensure_worker_secret() is create-if-absent and returns only the secret NAME, never the value.",
    "source_refs": "fleet/controller/state_sync.py:32-156;fleet/controller/docker_driver.py:34-155;fleet/controller/secrets.py:50-121",
    "verify_method": "cd fleet/controller && python3 -m pytest -q tests/ (tests/test_state_sync.py — rehydrate/mirror success/fail/fail-soft, exclude-regex validity, bind-mount-dir creation, src/dst ordering)",
    "notes": "PER_INSTANCE_SECRETS defaults OFF — ensure_worker_secret is only called when enabled (controller.py:315; tests/test_controller.py::TestDoLaunch::test_launch_skips/ensures_worker_secret). secrets.py exercised via the fake; live gcloud is GCP-gated | Live gcloud is GCP-gated (out of hermetic scope); exercised via fakes. PER_INSTANCE_SECRETS defaults OFF so ensure_worker_secret only called when enabled — verified.",
    "p2_test_status": "pass",
    "p2_evidence": "test_state_sync.py -> 14 tests pass (`pytest -k 'TestStateSync or test_state_sync'`): rehydrate/mirror success/fail/fail-soft, exclude-regex validity, bind-mount-dir creation, src/dst ordering. Read state_sync.py: makedirs source (Bug C5 uid alignment) then rsync GCS->local; mirror local->GCS; single Python-regex --exclude for lock/WAL/journal/SingletonLock. SecretManager.ensure_worker_secret returns only the NAME (controller.py:315; TestDoLaunch::test_launch_skips/ensures_worker_secret). DockerDriver injectable run_cmd.",
    "p3_fix_status": "n/a"
  },
  {
    "id": "CTRL-13",
    "area": "fleet-controller",
    "feature": "Config loading, secret redaction, systemd & run.sh entry",
    "user_story": "As an operator, I can configure the controller entirely via env vars and run it under systemd or run.sh so that required secrets fail fast at startup and never leak into logs.",
    "expected_behavior": "ControllerConfig.from_env() requires CONVEX_URL, WORKER_SECRET, IMAGE (ValueError if missing), parses HOSTS to a tuple, and applies typed defaults (poll 3s, stale_ttl 90s, mirror 300s, headroom 30%, capacity 50, max_launch_failures 3, per_instance_secrets off). WORKER_SECRET value is never stored on the frozen config; redacted() masks it. run.sh exec's controller.py (loading a dev .env if present) and forwards --once; the systemd unit runs as user hermes-fleet with EnvironmentFile, Restart=always, NoNewPrivileges=true.",
    "source_refs": "fleet/controller/config.py:13-151;fleet/controller/run.sh:1-17;fleet/controller/hermes-controller.service:1-22",
    "verify_method": "cd fleet/controller && python3 -m pytest -q tests/ (tests/test_controller.py::TestCapacityConfig::test_capacity_per_host_from_env exercises from_env; conftest test_cfg builds a real ControllerConfig); plus `./run.sh --once` as the documented single-pass smoke.",
    "notes": "adversarial=real (operator footgun; env file ships 240 so only bare runs hit it). FIX=raise code default to 240 to match docs | FIXED: default now 240; controller 83 passed.",
    "p2_test_status": "fail",
    "p2_evidence": "from_env happy/required paths covered: TestCapacityConfig::test_capacity_per_host_from_env exercises from_env, conftest test_cfg builds a real ControllerConfig (19-test placement+capacity set green). Read config.py: _require raises ValueError for missing CONVEX_URL/WORKER_SECRET/IMAGE (:126-130); HOSTS parsed to tuple; WORKER_SECRET validated but NOT stored (:127-128); redacted() at :101 masks it; run.sh + hermes-controller.service exist (NoNewPrivileges=true, Restart=always). BUT config.py:145 sets stale_ttl_s default = 90.0, while .env.example:24 ships STALE_TTL_S=240 and README.md:32/71 recommend 240 (>= worst-case Hermes turn).",
    "error_found": "config.py:145 STALE_TTL_S code default=90.0 contradicts .env.example:24 (=240) + README.md:32 ('set 240 >= worst-case Hermes turn'); running the controller bare (no env file) can relaunch a healthy container mid-turn and drop an in-flight reply.",
    "severity": "low",
    "error_type": "logical",
    "p3_fix_status": "done",
    "p3_fix_ref": "fleet/controller/config.py:145 STALE_TTL_S 90->240; +TestCapacityConfig.test_stale_ttl_default_matches_docs; conftest deps pins 90 for decision tests",
    "p4_retest_status": "pass"
  },
  {
    "id": "CTRL-14",
    "area": "fleet-controller",
    "feature": "Fleet image build-gate, headless-Chrome entrypoint & offline multi-tenant smoke",
    "user_story": "As an operator, I can build the per-tenant image with a hard in-image test gate and prove offline that two tenants never cross-contaminate so that a broken worker never ships and tenant isolation is verified without GCP.",
    "expected_behavior": "Dockerfile.fleet runs a HARD build-time pytest gate (network-free subset, no '|| true', strict-markers) so a broken worker fails the build; it runs the worker as non-root uid 10001, uses tini as PID 1, and CMD=./fleet-entrypoint.sh which (unless BROWSER_HARNESS_ENABLED=0) launches headless Chromium on CDP :9222 under HERMES_HOME then exec's worker.py. fleet/local/docker-compose.yml stands up mock_outbound + worker_alice (u_alice) + worker_bob (u_bob) + seed, asserting alice->alice / bob->bob with zero cross-routing via isolated per-tenant volumes.",
    "source_refs": "lab/docker/Dockerfile.fleet:169-256;lab/docker/fleet-entrypoint.sh:1-32;fleet/local/docker-compose.yml:35-184;fleet/local/seed.py:1-40",
    "verify_method": "Offline: cd fleet/local && docker compose up --build --abort-on-container-exit --exit-code-from seed (exit 0 = correctly routed). Hermetic logic without docker: cd /Users/saveliy/Documents/Amyris && python3 -m pytest -q fleet/local/test_harness_logic.py (MockStore isolation + negative cross-route rejection + vacuous-pass guard). Image gate is the RUN pytest inside docker build.",
    "notes": "lab/docker/Dockerfile (simpler base image) also has its own build-time `python -m pytest -q` gate at line 20. INSTALL_HERMES=0 keeps the gate offline; =1 adds the Hermes+Chromium+browser-harness layers | Late 'ConnectionError to mock_outbound:8800' lines are the two workers losing the mock after seed exits — NOT a failure (per Lead baseline). lab/docker/Dockerfile also has its own build-time pytest gate at line 20; INSTALL_HERMES=0 keeps gate offline.",
    "p2_test_status": "pass",
    "p2_evidence": "Offline multi-tenant isolation smoke PASSED (Lead baseline: docker compose up --build --exit-code-from seed -> seed exit 0, alice->alice/bob->bob no cross-route). Hermetic logic: `python3 -m pytest -q fleet/local/test_harness_logic.py` -> 25 passed (MockStore isolation + negative cross-route rejection + vacuous-pass guard). Dockerfile.fleet:169-256 has a HARD build-time pytest gate (no '|| true', strict-markers), non-root uid 10001, tini PID 1, CMD=fleet-entrypoint.sh launching headless Chromium CDP :9222 then exec worker.py.",
    "p3_fix_status": "n/a"
  }
]