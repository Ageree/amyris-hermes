"""Feature 5 — create personalized AI apps backed by Turso.

provisioner.TursoProvisioner  — create / list / delete per-app Turso databases
generator.generate_app        — spec -> provisioned DB + tables + app manifest
libsql_http                   — dependency-free libSQL HTTP (pipeline) client
"""
