{
  "name": "saved-content-control-plane",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Always-on Convex control plane: Sendblue inbound webhook + durable message queue. The brain (Hermes + browser) polls this; it never has to be publicly reachable.",
  "scripts": {
    "dev": "convex dev",
    "deploy": "convex deploy",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run --reporter=verbose",
    "test:coverage": "vitest run --coverage",
    "test:drainer": "for test in drainer/*.test.mjs; do node \"$test\"; done"
  },
  "dependencies": {
    "@auth/core": "^0.41.1",
    "@convex-dev/auth": "^0.0.94",
    "convex": "^1.40.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
