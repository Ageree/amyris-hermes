# Phase-1A skeleton webhook bridge deps (install into the LAB venv, NOT ~/hermes-agent/venv).
# The bridge only subprocesses ~/hermes-agent/venv's python; the skeleton itself
# must not pollute that venv. Verified working on the lab venv (Python 3.9.6).
# Pinned to minimum safe versions to mitigate supply-chain risk (new-version attacks).
fastapi>=0.115.0,<1.0
uvicorn>=0.49.0,<1.0
requests>=2.32.0,<3.0
httpx>=0.27.0,<1.0
pytest>=8.0,<9.0
pyyaml>=6.0.1,<7.0  # worker seeds the tenant config.yaml (browser-harness flip)
